# 상세 이력과 저장 완료 응답의 정합성

- 실행일: 2026-09-21.
- 시작 제품 커밋: `67ea22ca127368abf6fd24f3a79d82823a560e40`.
- 범위: 기존 상세 화면의 이력 응답 연결과 저장 중 화면 전환 시 입력 보존. 모델·원본·API·저장소 구조는 변경하지 않는다.

## 수정 전 실제 재현

20:46 KST에 공유 서비스와 분리된 후보 웹에서 브라우저 요청 가로채기로 응답 순서만 제어했다.

1. 사진 A의 이력 응답을 지연하고 창을 닫았다. 포인트 B를 열어 B의 등록 이력을 확인한 뒤 A 응답을 보냈더니, B 창에 A의 사진 업로드·AI 이력 3개가 표시됐다.
2. 사진 A의 저장 요청 응답을 지연하고 A 창을 닫았다. 사진 B를 열어 변경 사유를 입력한 뒤 A의 성공 응답을 보냈더니 B 창이 닫혔다.

저장 요청은 실제 서버로 전달하지 않은 모의 응답이다. 관련 사진의 API 데이터는 전후 동일했고 사진 수 10개도 그대로였다. 실제 DB 쓰기나 새 사진 업로드 없이 화면의 응답 처리 문제를 재현했으며 요청 가로채기는 검사 후 해제했다.

## 변경한 동작

- 사진·포인트 이력을 공통 화면 상태에서 분리하고 각 상세 창이 자기 ID의 이력을 조회한다. 창을 닫거나 대상을 바꾸면 요청을 취소하고 이전 응답·오류 알림을 무시한다. 열린 창의 실제 실패에는 기존 오류 안내를 표시하며 10초 응답 제한을 유지한다.
- 저장 완료 알림과 창 닫기는 요청을 시작한 모달 인스턴스가 아직 유효할 때만 수행한다. 목록 갱신을 기다리는 동안 창이 바뀔 수도 있으므로 닫기 직전에도 다시 확인한다. 사진 저장·판독 재요청, 포인트 저장, 계획 등록에 같은 규칙을 적용했다.
- 창을 닫는 동작으로 이미 서버에 전달된 저장 요청을 취소하거나 되돌리지는 않는다. 늦은 응답이 새로운 창을 닫거나 새 입력을 지우는 문제를 방지한다.

## 실제 검증과 공유 반영

- 타입 검사·후보 및 운영 프로덕션 빌드와 독립 소스 검토를 통과했다.
- 20:52~20:53 KST 후보 빌드 `T7stOJVGyEBFeKVD1VBhc`에서 같은 지연 응답을 재현했다. 포인트 B의 이력은 바뀌지 않았고 이전 사진의 늦은 오류 알림도 없었다. 사진 A 저장 완료 뒤 사진 B의 상세 창 1개와 미저장 입력이 유지됐다.
- 같은 완료 처리 규칙을 사용하는 포인트 저장·계획 등록도 이전 응답 뒤 새 창과 입력을 유지했다. 실제 DB 쓰기는 없었으며 사진 10개·포인트 24개·계획 4개의 관련 API JSON은 전후 동일했다. 요청 가로채기는 검사 종료 후 해제했다.
- 현재 열린 사진 창의 이력 조회에 500 응답을 주면 한국어 오류 안내와 `alert` 역할이 유지됐다. 가로채기를 해제하고 다시 열면 실제 이력 3개가 나타났다. 현재 창의 모의 저장 성공에는 정상적인 창 닫힘과 성공 `status` 알림이 유지됐다. 저장 검사는 모의 응답으로, 새 서버 저장 검증을 수행한 것은 아니다.
- 21:00 KST 공유 웹만 운영 빌드 `LX8kID2yxjTpPqEXcxtge`로 교체했다. 후보와 운영의 수정 소스 해시는 동일하다. 로컬·외부 URL의 HTML에서 새 빌드를 확인했고 루트·건강 검사 HTTP 200, API 및 모델 준비 상태를 확인했다. 외부 브라우저의 검사 계획 화면도 정상 표시됐다. 이 확인은 읽기 접속이며 새 공개 데이터를 쓰지 않았다.
- 기존 API·DB·MinIO·모델·터널은 유지했고 이전 웹 아티팩트는 복구용으로 보존했다. 후보 웹과 별도 운영 미리보기는 확인 후 종료했다.

같은 항목을 여러 화면에서 수정할 때의 서버 버전 충돌 검사는 다음 별도 작업이다. 이 변경만으로 동시 저장 충돌까지 해결했다고 주장하지 않는다.
