# 플랫폼 기반 구현 기록

- 시작: 2026-09-21 16:36 KST
- 시작 커밋: `46f8cd23b6032741d08310b0108b205df5ae9787`
- 브랜치: `codex/jun/platform-foundation`
- 범위: Next.js 화면, Mac API, MySQL/MinIO Compose, 업로드와 이력 관리, 로컬 실행 및 배포 준비
- 승인된 실행 프롬프트: 허브에서 전달한 "지금 시작 승인"에 따라 기반 라이브러리 설치와 작은 수직 기능부터 실행. 목표 원문은 Git 제외 로컬 기록으로 보존한다.
- 기존 원본·분리 목록·test10·모델 가중치를 사용하거나 변경하지 않는다. 기능 검증은 공개 demo 또는 합성 입력을 사용한다.
- 최초 설치 환경: Node 25.6.0, npm 11.9.0, Docker CLI 29.4.0.
- 최초 환경 실패: npm 기본 캐시 쓰기 EPERM. 프로젝트 `.cache/npm`을 지정해 전역 설정 변경 없이 복구.
- Docker 최초 상태: OrbStack 소켓 없음. 실행 시도와 결과는 이후 기록한다.

실제 검증 결과와 실행 주소는 검증 후 추가한다. 기능·모델 성능·실측 두께 성능·사람 검수는 각각 구분한다.

## 16:56 1차 실행 확인

- Node 25.6.0 / npm 11.9.0 / Next 16.3.5 / React 19.3.0 / TypeScript 7.0.2 설치.
- `npm run typecheck`, `npm run build` 성공. Next `next dev`는 파일 감시 `EMFILE`로 404가 발생하여 종료하고 운영 빌드로 기동했다.
- `http://127.0.0.1:3000` 실제 브라우저에서 대시보드·예시 3D·업로드 버튼 렌더 확인. 데이터 API는 아직 준비 중으로 화면에 연결 오류가 정확히 표시된다.
- Docker 29.4.0 / Compose 5.1.2 실행 확인. 공식 문서의 MinIO 2025-09-06 태그가 manifest 오류이므로 실제 manifest가 있는 공식 `quay.io/minio/minio:RELEASE.2025-07-23T15-54-02Z` 선택. MySQL8.4와 함께 다운로드 중.
- 독립 API 기술 검토 후 묶음 데이터의 단일 트랜잭션 처리, 처리 중 큐 3분 후 복구, MinIO 버킷 존재 여부 health 검사 수정.
- 모델 API 계약 `POST /predict`, multipart `image`, `grade/confidence/model_version/preprocessing_version` 연결 구현. 원래 AI 결과와 사람 수정 등급 분리.
- 사용자 최신 합의에 따라 두께 자료·두께 예측은 이번 범위에서 제외한다.
- `npm audit`: MinIO 전이 의존성의 moderate 4건, high/critical 0. 자동 강제 하위 버전 변경은 하지 않았다. 외부 공개 전 별도 검토가 남는다.

## 17:00~17:10 실제 저장·판독·보존 검증

- MySQL8.4·MinIO2025-07-23 기동. API `/api/health`에서 저장소 및 최종 모델 ready=true.
- 공개 demo5장을 두 번 사용하는 묶음 업로드 10장: **1.385초**. 업로드 시작부터 실제 모델 10장 완료까지 **16.487초**. 학습/평가 표본 추가가 아니다.
- 원본 공개 demo 바이트 → API 업로드 → MinIO → API 이미지 재조회: **10/10 SHA-256 동일**. 안정 ID·포인트·object key·원본/재조회 해시는 Git 제외 `platform-smoke.local.json`에 보존.
- 브라우저에서 실제 사진 10장·숫자 등급·원래 AI 모델/전처리 버전을 확인. 특정 demo의 원래 AI3을 보존하며 사람 수정4·재촬영 사유·라벨링 후보 저장 확인.
- 독립 검증 담당이 별도 가상 포인트/demo10으로 실제 사진·캘린더·이력·관리·TA 경로 검수. 해당 작업과 합의한 시점에 쓰기를 멈추고 MySQL·MinIO·API를 정상 재시작.
- 재시작 전후 **사진20·포인트2·계획1, 모든 이력 및 원본 바이트 해시의 46개 비교 항목 동일**. 테스트 완료 로그뿐 아니라 DB 재조회와 이미지 바이트 재조회로 확인.
- API 오류·출처·AI 덮어쓰기 방지, COMMIT 전 실패/COMMIT 응답 손실 구분, 공개 demo 혼합 묶음 거절: **6개 검사 통과**.
- 실제 유효 PNG 합성 입력과 승인 JPG를 섞은 공개 모드 요청: **422**, MinIO 객체 **20 → 20**; DB 목록도 불변. 파일명을 demo와 같게 해도 비허용 바이트는 거절.
- Docker 컨테이너에서 `host.docker.internal:8001/health` 최종 모델 연결 확인. 전체 앱 Docker 이미지 빌드는 아직 미검증이며, 현재 웹/API는 Mac Node 프로세스로 실행 중이다.
- 모델 성능: 전달받은 최종 모델 `mobilenetv3small-ft-20260921T074822Z`의 test60%는 합격선70% 미달. 실제 서비스 연결과 정확도 합격을 구분해 UI에도 표시한다. 추가 튜닝은 수행하지 않았다.
- 기술 검토 후 COMMIT 응답 손실 시 DB 저장 여부가 불확실하면 원본 객체를 보존하도록 수정. 응답 전송 오류가 보상 삭제로 이어지지 않게 분리했다.
- 실제 브라우저 캡처: `platform-dashboard.local.png`, `platform-photo-ai.local.png` (Git 제외). 공개 demo만 표시한다.

## 17:13 공개 모드 로컬 인수

- `PUBLIC_DEMO_ONLY=1`, `NEXT_PUBLIC_DEMO_ONLY=1`로 로컬 실행. 임의 파일 입력 0개, 승인된 시연 사진 선택만 표시되는 것을 실제 브라우저에서 확인했다.
- 화면의 시연 사진5장 선택 → 위치 미확인으로 업로드 → 실제 AI5장 완료 → 사진/등급/한글 이력 재조회 확인. 현재 로컬 DB는 시연 사진25장(공개 demo 복제), 포인트2개, 계획1개다.
- 대시보드의 사진·포인트·계획·health 4개 API를 같은 Next 주소로 조회: 전부200, 병렬 총68.12ms(로컬 1회 측정). 이는 비개발자 조작 시간이나 실환경 네트워크 성능 보증이 아니다.
- 모델의 `confidence`가 보정되지 않은 softmax 점수라는 담당 설명에 따라 화면·이력의 '신뢰도'를 **'모델 점수(보정 전)'**로 수정했다. 정답 확률이나 설비 안전 확률을 의미하지 않는다.
- 최종 운영 핸들과 커밋은 허브에 별도 전달한다. Cloudflare/Vercel/Sites 외부 배포는 이 작업에서 실행하지 않았다.

## 17:32 이후 공개 업로드·시연 지연·검수 보완

- 추가 승인에 따라 공개 가능한 사진의 일반 파일 선택을 허용했다. `PUBLIC_DEMO_ONLY=0`, `NEXT_PUBLIC_DEMO_ONLY=0`, 공개 업로드 안내 플래그 두 개는 1로 운영한다. 안내 플래그는 서버 접근 통제 스위치가 아님을 실행 문서에 명시했다.
- 독립 QA가 실제 외부 브라우저 파일 선택으로 공개 demo 복제 1장을 새 포인트에 업로드하고 실제 AI 완료를 확인했다. 원본·DB SHA·API·MinIO·외부 HTTPS 재다운로드 바이트가 동일했다.
- 시연 버튼은 같은 포인트/SHA의 기존 ID·AI·사람 수정·이력을 재사용한다. 일반 파일 선택은 같은 바이트도 새 검사 기록을 만든다. 기존 중복 기록을 삭제하지 않는다.
- 첫 시연 화면 검수에서 5장 다운로드가 2분 이상 지연되고 준비 단계가 ‘업로드 0%’로 보이는 실패를 확인했다. 서버의 승인 파일을 포인트 ID JSON만으로 불러오는 경로로 바꾸고 실제 저장 코드·이미지 검증·잠금을 공유했다.
- 변경 후 외부 JSON 요청은 16바이트, 200/existing 5장 응답 **1.002초**였다. QA의 실제 버튼 선택 → 기존 상세 열기는 **2.753초**였다. 기존 ID와 필드가 그대로였다.
- 카드에 원본 대신 최대 320×200 WebP 미리보기를 사용했다. 공개 demo5 미리보기는 각각 **4,864~6,680바이트**, 외부 요청 **0.371~0.583초**였고 원본 SHA는 모두 보존됐다.
- QA의 외부 일반 파일 10장(13,139,268바이트)은 저장과 실제 AI done10을 확인했다. 화면 저장 알림은 **29.1초**였으나 파일 선택부터의 전체 시간 측정은 도구 중단으로 불완전했다. 이를 전체 30초 기준 통과로 표시하지 않는다.
- QA의 warm 브라우저 1회 측정: 대시보드 새로고침 → 수치 **1.478초**, 이미지까지 **1.483초**. 계획 화면 열기·자동 입력·저장·목록 반영 **0.928초**. 사람의 읽기·입력 시간을 포함한 사용성 검증이 아니다.
- 공통 API는 기본 10초 제한, 시연 요청은 30초, 일반 파일 전송은 180초 제한을 적용했다. 네트워크 단절·HTML 5xx·잘린 JSON을 한국어 오류로 표시하고 쓰기 응답 손실 시 목록에서 반영 여부를 확인하도록 안내한다.
- 예시 맵의 배열 순서 좌표를 설비·랙 기반의 안정적인 모형 슬롯으로 바꿨다. 실측 좌표가 아니며 설비/랙 미확인은 표시하지 않는다. 선택 ID는 기본 12개 밖이어도 표시하며 마커·키보드·하단 버튼이 동일 포인트 상세로 연결된다.
- 공통 오류 안내 3개, 맵 규칙 3개를 포함해 **13개 검사**, typecheck, production build 통과. 독립 소스 검토의 목록 갱신 timeout 및 썸네일 실패 헤더 지적을 반영했다.
- 최종 공유 빌드 `vmIQA4WMXbo_KKkoz5t4G`: 외부 health 200, 저장소/최종 모델 ready. 실제 외부 브라우저에서 DEMO-101/랙2 선택 → 마커1개 → 같은 포인트 상세와 연결 사진15장 조회를 확인했다.
- Docker 전체 웹/API 이미지를 빌드하고 별도 `inspection-platform-smoke` 프로젝트의 MySQL·MinIO·API·웹을 3300/3400에 기동했다. 운영 3000/4000·DB·터널은 검증 환경과 분리했다. 모델은 별도 Mac 프로세스이며 완전한 모델 Compose 통합을 주장하지 않는다.
- 격리 환경에서 새 포인트의 시연 요청 4개를 동시에 보내 **409/409/409/201**, 이어 재시도는 **200/existing 5개**였다. 새 사진은 정확히 5개만 생성됐다.
- 격리 QA는 승인 demo 해시 밖의 64×64 합성 PNG를 일반 업로드해 201을 확인했다. 잘못된 PNG는 422·재선택 안내와 전체 목록 불변을 확인했다. 회사 원본 사진을 추가 사용하지 않았다.
- 별도 모델 프록시에서 의도한 503을 반환해 실제 `error`, AI 결과 없음, 실패 이력·재시도 버튼을 확인했다. 이후 동일한 고정 모델로 연결을 복구하고 **같은 사진 ID**의 UI 재시도로 done·실제 AI3·원래 모델 버전을 확인했다. 업로드/시작/실패/재요청/시작/완료 **6개 이력**을 보존했다.
- MySQL·MinIO·API·웹 전체를 정상 중지한 뒤 최신 Docker 이미지로 기동했다. Docker Node **24.21.0**, 사진6·포인트1·계획0의 전체 메타데이터·이력·원본 해시 **16개 비교 항목 동일**. 볼륨을 삭제하지 않았으며 공개 운영 데이터는 재시작 대상이 아니었다.
- 독립 QA도 사진6 전필드·대상 이력6·동일 ID 원본 바이트의 재시작 전후 일치를 확인했다. 이후 격리 API 중단에 따른 실제 계획 POST 500, 격리 웹 중단에 따른 실제 네트워크 실패를 각각 유발해 한국어 오류와 ‘저장된 목록에서 반영 여부 확인 후 재시도’ 안내를 화면에서 확인했다. 이 오류를 저장 성공으로 표시하지 않았다. HTML 502와 잘린 JSON·시간 초과는 별도 자동 검사로 확인했다.
- 격리 DB에 가상 포인트20개를 추가해 등록21개 중 기본 12개에 없는 포인트를 실제 브라우저에서 선택했다. 선택 마커1개·같은 ID의 상세·연결 demo 사진1장을 확인했다. 새 포인트 추가로 등록22개가 된 뒤에도 선택 마커의 SVG 위치 `translate(475 86)`은 동일했다. 기능 검증이며 실제 설비 좌표 정확도 검증은 아니다.
- 18:06:25 KST 최종 외부 계측 1회: 공개 demo 복제10개(13,139,268바이트), 파일 선택 완료 후 업로드 클릭 → 저장 모달 닫힘 및 새10행 표시 **13.0127초**. POST 전송/응답 **12.3843초**, 클릭 → 10장 등급 UI **30.1645초**, POST 완료 → 등급 UI **17.4515초**였다. 10개 새 ID의 실제 backend done과 화면을 확인했다. 이 조건에서 저장30초/판독3분 기준은 충족했으나, 파일 선택·읽기·사람 판단을 포함한 전체 시간이나 능동 작업1분 기준을 입증하지는 않는다.

## 완료 구분과 남은 검수

- 기능 기반: 로컬 업로드·사진 원본 조회·실제 AI·사람 수정·재촬영/라벨링·포인트 보수/관리/TA·캘린더·변경 이력·정상 재시작 보존 확인.
- 모델 성능: 최종 test60%로 기준70% 미달. 기능 성공으로 성능 합격을 대신하지 않는다.
- 비개발자 사용자 검수: 아직 미실시. 현재 검수는 에이전트의 실제 브라우저 조작·API/저장소 확인이다.
- 외부 URL은 배포 담당의 Cloudflare 터널을 사용한다. 공개 업로드·미리보기·시연 재사용을 실제 외부 브라우저에서 확인했다. 웹/API 전체 Compose 빌드·격리 기동도 확인했으며, 모델은 Mac의 별도 프로세스다.
- 프로젝트 전역 설정·회사 원본300장·분리 목록·고정 test·모델 가중치는 이 작업에서 수정하거나 외부 전송하지 않았다.
