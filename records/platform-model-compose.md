# 선택형 모델 Compose 실행 검증

- 실행일: 2026-09-21, 18:15~18:25 KST.
- 시작 제품 커밋: `04b8cd578e83299f07a794d5aa3a07f8e5a17bb7`.
- 요청: 사용자의 “하던 작업도 계속 해줘”와 허브가 배정한 선택형 5서비스 구성. 현재 공유 서비스를 유지하면서 웹·API·MySQL·MinIO·동결 모델의 함께 시작·종료와 데이터 보존을 확인한다.
- 소스 의존성: [모델 작업 PR #6](https://github.com/junhang-dev/inspection-image-platform/pull/6). 이 제품 체크아웃에는 아직 `ml/`이 통합되지 않았다. 다른 작업의 파일을 복사하거나 커밋하지 않았다.

## 변경한 동작

`compose.model.yaml`을 기존 구성에 추가할 때만 모델 서비스를 포함한다. API는 Docker 내부 `http://model:8001`을 사용하고, 모델이 동결 계약 검사에 통과한 후 시작한다. 가중치·계약 파일은 Git 제외 `.env`의 절대경로로 지정하며 읽기 전용 bind mount 두 개로만 연결한다. 모델에는 호스트 공개 포트가 없다.

모델 컨테이너는 사용자 `10001:10001`, 읽기 전용 루트, 임시 `/tmp`, 권한 제거, CPU 2개·메모리 2GiB 제한으로 실행한다. 기존 4서비스 명령과 Mac 모델 연결 설정은 유지했다. 실행 및 준비 상태 확인은 [운영 안내](platform-runbook.md#선택-사항-모델까지-함께-시작종료)에 기록했다.

## 실제 실행한 검사

별도 Compose 프로젝트 `inspection-platform-smoke`의 웹 3300/API 3400과 분리된 MySQL·MinIO 볼륨을 사용했다. 공유 중인 3000/4000/8001·터널은 교체하지 않았다. 공개 승인된 `image/demo/demo-01.jpg`만 새로 한 장 업로드했고, 원본 300장·분리 목록·고정 test를 사용하지 않았다.

| 검사               | 실제 결과                                                                                                                 |
| ------------------ | ------------------------------------------------------------------------------------------------------------------------- |
| 기본 구성 호환     | 모델 경로 두 값이 비어 있어도 기존 `compose.yaml` 검증 통과                                                               |
| 필수 모델 경로     | 추가 구성에서 경로가 없으면 구성 단계에서 거절                                                                            |
| 5서비스 시작       | 기존 로컬 이미지를 `--no-build --pull never --wait`로 기동. 5개 실행 및 모델 `healthy` 확인                               |
| API와 내부 모델    | API 컨테이너의 `MODEL_API_URL=http://model:8001` 및 실제 health 응답 확인                                                 |
| 제한 적용          | 모델 읽기 전용 루트·비루트 사용자·권한 제거·읽기 전용 파일 두 개·호스트 포트 미공개를 Docker inspect로 확인               |
| 신규 사진 처리     | 승인 demo 1장 업로드 → MinIO 저장 → 실제 모델 판독 `done`까지 2.101초. 업로드 전 파일과 재조회 원본 SHA-256 동일          |
| 기존 Mac 결과 비교 | 동일 사진의 등급 4·모델 버전·전처리 버전 일치. 모델 점수 차이 0으로 사전에 정한 허용오차 `1e-5` 이내                      |
| 전체 중지·재기동   | 5개 서비스를 `stop` 후 `up`으로 재개. 볼륨 삭제 없음                                                                      |
| 저장 데이터 보존   | 재시작 전후 사진 7개·포인트 22개·계획 0개, 전체 메타데이터·이력·원본 해시 39개 비교가 모두 동일. 계획 목록은 빈 상태 비교 |
| 재시작 후 모델     | 계약 기반 healthcheck 종료 코드 0, API `ready=true` 및 `model.ready=true`                                                 |
| 기존 공유 서비스   | 18:25 KST 외부 웹/API HTTP 200, 기존 Mac 모델 8001 `ready=true`. 신규 격리 업로드 전후 공유 검사 목록도 동일              |

실제 검증 이미지 `inspection-model:local`의 ID는 `sha256:7cccccbf011893a98eff0afa5c449741e0610ad940486f62f80f66f8b66aa76f`이다. Docker Compose 버전은 5.1.2였다. 동결 계약과 시작·재시작 후 health가 아래 세 값을 동일하게 보고했다.

- 모델: `mobilenetv3small-ft-20260921T074822Z`
- 전처리: `rgb-first-frame-exif-full-resize224-imagenet-v1`
- 체크포인트 SHA-256: `21245b2f2d8b478b7b2762c51e7882692babd062eb1de8ec305cb090a7906753`

원시 증거는 Git 제외 `records/platform-model-compose.local.json`과 `records/platform-five-persistence.local.json`에 보존했다. 독립 기술 검토에서 병합 규칙·볼륨 보존·모델 연결·마운트·포트·명령의 차단 결함은 없었으며, 실제 준비 상태 확인 절차와 모델에만 적용되는 자원 제한 설명을 보완했다.

## 남은 구분

- 최초 검증은 이미 빌드된 로컬 모델 이미지로 수행했다. 이후 별도 임시 폴더에서 아래의 정확한 후보 소스 전체 빌드·기동을 추가 검증했다. 현재 작업 브랜치나 `main`을 병합·전환한 것은 아니다.
- 현재 외부 공유 서비스는 Mac 웹/API 및 별도 Mac 모델을 계속 사용한다. 선택형 구성을 공유 서비스로 전환하지 않았다.
- 이 검사는 실행·연결·데이터 보존 검증이다. 모델 성능 향상, 새로운 test 평가, 비개발자 사용자 검수 또는 재학습 완료를 의미하지 않는다.

## 추가: 후보 소스 전체 빌드와 실제 추론

2026-09-21 18:47~18:58 KST에 Git 담당이 준비한 별도 임시 폴더의 후보 소스를 수정 없이 검증했다. 결합한 정확한 입력은 다음과 같다.

| 입력          | 커밋                                       |
| ------------- | ------------------------------------------ |
| 기준 main     | `64221ad85b51c0c19a8e91744ee263c437bbc910` |
| 제품 PR #7·#8 | `09835fa680b31d984db7687b49ce00fb49dd6e1b` |
| 모델 PR #6·#9 | `3d1f0d871ac76d449a9f0f31012df1eb73309546` |
| 허브 PR #5    | `9816422f1185036affb27ad92dad198354653805` |
| QA PR #4      | `703504086cfe7e1bad8c1c62d9e135fbdb2898c6` |

최종 Git tree는 `2b59a83c87e18058c84e4f186d0a7fb5597a6e02`다. 소스 89개 파일의 SHA-256이 인계 명세와 빌드 전후 모두 일치했다. 원래 `.dockerignore`를 그대로 사용했고, 실제 `.env`·원본 사진·가중치·토큰·DB·로컬 상태·의존성·Git 메타데이터를 소스 폴더에 넣지 않았다. 런타임 설정과 로그는 빌드 context 밖에, 가중치와 계약은 읽기 전용 mount에 두었다.

첫 빌드는 API의 `npm ci --omit=dev`에서 319초 후 `EIDLETIMEOUT`으로 실패했다. Docker 안의 npm 레지스트리 ping·패키지 메타데이터 HTTP 200을 확인하고, 소스·lockfile을 바꾸지 않은 단일 재시도로 웹·API·모델 이미지 빌드를 완료했다. 이전 성공한 Docker 캐시를 사용했으며 캐시 없는 빌드라고 주장하지 않는다. Next 프로덕션 컴파일·정적 페이지 생성도 통과했다.

| 새로 빌드한 이미지                            | 이미지 ID                                                                 |
| --------------------------------------------- | ------------------------------------------------------------------------- |
| `inspection-platform-sourcecheck-api:09835fa` | `sha256:aebc2cd3a7c972b4a141597b8f3b9ceaf8fe4c57bf5a6b58e795946c27350be6` |
| `inspection-platform-sourcecheck-web:09835fa` | `sha256:5d474509ed7ca08333bfd8272a0491cab16ae3d6c7e16f43dc73395c508401cd` |
| `inspection-model-sourcecheck:3d1f0d8`        | `sha256:e7d23ad75928ae18c6c6d267ed9c7fd585724792e090b8a979ea15e32e6ae962` |

새 이미지로 같은 격리 프로젝트의 5서비스를 기동했다. 기존 볼륨의 사진 7개·포인트 22개·계획 2개를 조회했고, 새 웹의 `http://127.0.0.1:3300/api`를 통해 승인 demo 1장을 업로드했다. 웹 → API → MinIO → 내부 모델의 실제 판독이 `done`으로 끝났으며, 원본 SHA-256과 기존 Mac 모델의 등급 4·모델 버전·전처리 버전·모델 점수(차이 0)가 일치했다. 계약 healthcheck도 위의 동결 버전·해시와 동일하게 종료 코드 0을 반환했다.

이미 통과한 전체 재시작·사용자 흐름 검사는 반복하지 않았다. 이번 결과는 후보 소스의 사전 통합 검증이며 `main` 병합, PR 병합 또는 현재 공유 서비스의 운영 전환 완료가 아니다. 첫 실패·재시도 로그와 실제 응답은 Git 제외 `records/platform-source-build.local.log`, `records/platform-source-build-retry.local.log`, `records/platform-source-build.local.json`에 보존한다.

검사 후 격리 5서비스만 중지하고 기존 볼륨을 보존했다. 19:00 KST 외부 공유 웹·API와 기존 Mac 모델은 HTTP 200 및 준비 완료를 확인했다. 별도 담당이 프록시 헤더 문제를 수정한 Label Studio도 기존 프로젝트 주소에서 로그인 화면으로 정상 연결됐다. 공유 서버·터널·모델·Label Studio의 운영 구성을 이 검증에서 교체하지 않았다.

Label Studio 프록시 수정 `aa44a44`는 위 후보 tree 이후 별도 담당이 수행한 로컬 운영 수정이다. 이번 5서비스 소스 빌드에 해당 후속 변경까지 포함됐다고 주장하지 않는다.

## 파인딩 조회 요구의 대응 범위

사용자의 “파인딩 사항 레포팅이나 대시보드에서 확인” 요구는 현재 대시보드 → 사진·판독 목록 → 사진 상세·이력 경로로 대응한다. 외부 실제 화면에서 전체 62장·시각적 3~5등급 38장·대기/진행 0장·재촬영 2장 집계가 API 데이터와 같았다. 최근 사진 5행에는 사진·설비/포인트·현재 등급·후속 관리·등록 시각이 표시된다. ‘전체 보기’에서 검색·필터로 대상을 찾고, 상세에서 원본·원래 AI 등급/보정 전 점수/버전·사람 판단·재촬영 사유·변경 이력을 확인한다. 실제 선택 사진의 API 이력 3개와 화면 이력 3개도 일치했다.

기존 대시보드 표시 시간 측정은 [제품 검증 기록](platform-foundation.md)에 있다. 이 확인은 별도 보고서 파일, 인쇄, 내보내기 기능의 완료를 의미하지 않으며 새로운 보고 형식을 추가하지 않았다.
