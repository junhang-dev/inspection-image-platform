# PlantPilot v2 업로드·가독성 후보 독립 QA

## 범위와 실행 버전

2026-09-21 22:54~23:07 KST, 격리 웹 3500/API 3400에서 업로드 큐·실패 복구·원본 보존·실제 모델 연결과 관련 화면을 확인했다. 기반 제품 커밋은 `0159062`, 웹 BUILD_ID는 `IH4Hic5nFa25IVgE76ubE`다. 웹 파일 해시와 실행 중 API 파일 해시를 별도로 보존했다. 제품 코드·공유 서비스·터널은 QA가 변경하지 않았다.

직접 실행한 API는 r2 이미지 `sha256:6db4137db6c2f0ec4048773846a11dc5fa06ce49b2537fadc11282b6b3e4383c`다. 실행 중 `server/uploads.mjs` SHA-256은 `3e4991f21b382a196b51ee229ab1bd3649359814bdd2f8b4b5d9950fed59fe5e`이며, 인계 manifest의 후속 파일 해시와 다르다. 따라서 아래 결과를 후속 r3의 직접 검증으로 간주하지 않는다.

제품 담당은 r3에서 느린 청크 수신 네 개 중 다섯 번째 요청 503, health 200/37.45ms, 연결 중단 후 일반 청크 200과 offset 진전을 보고했다. 제공된 r3 서버 해시 일곱 개를 읽었고 QA r2와 달라진 파일은 `server/uploads.mjs` 한 개였다. r3 이미지는 `sha256:8d97bdad51d68b8d8eb4718ae0a3a7c0560256fd5b01f5211c7e7b7cdab3e9e3`다. 이는 제품 담당의 실행 결과이며 QA가 같은 부하 검사를 반복한 결과는 아니다. 담당자는 23:13 공유 반영을 보고했고 QA는 공유 주소의 HTTP 200과 공개 빌드 `tIy0dhwgYULnyihTt-VEV`를 직접 확인했다. 공유 사이트에서 업로드 검사를 반복하거나 새 자료를 쓰지는 않았다.

모델 이미지 `sha256:f6d6a540208fa36de1a88c77bcfa5b1861633624fec8d422b3e59fb71d91fc7e`, 모델 소스 `90dc40209ded6b330552e20079780aa7ac90f635`를 사용했다. 실제 결과의 모델 버전은 `mobilenetv3small-ft-20260921T074822Z`, 전처리는 `rgb-first-frame-exif-full-resize224-imagenet-v1`이다.

## 입력과 실제 저장량

- 첫 선택은 12개, 60,938,913bytes다. 승인된 `image/demo/demo-01.jpg`의 바이트 그대로인 복제본 11개와 회사 자료와 무관한 합성 PNG 한 개를 사용했다.
- 합성 PNG는 고정 시드 `20260921`의 RGB 난수 픽셀로 생성했다. 3072×3072, RGB 8bit, 정확히 28,323,297bytes이며 패딩으로 파일 크기를 늘리지 않았다. PNG 구조와 실제 디코딩을 확인했고 저장 전에 원본을 축소·재인코딩하지 않았다.
- 이어서 같은 demo의 긴 파일명 복제본 한 개를 별도 신규 등록했다. 최종 13개는 63,903,969bytes이며 서로 다른 사진 UUID를 갖는다. 같은 SHA의 신규 등록과 같은 업로드의 재시도를 구분했다.
- demo 복제본은 각각 2,965,056bytes, SHA-256 `0510d0827704d8ec297bdecdebfd2eb7f65c44521e662fa8532036e47a610838`이다. 합성 PNG의 SHA-256은 `f58e6a8bb9d828f2fee996abaf8590b45963808e72f234582e5905db15fa4190`이다.

기존 QA 계획·포인트에 연결했고 새 계획·포인트는 만들지 않았다. 사진 목록은 17→30개이며 실행 전 17개 사진의 전체 응답 필드가 마지막 조회와 동일했다. 원본 300장·고정 test·분리 목록은 사용하거나 변경하지 않았고 복제본을 학습·성능 표본에 추가하지 않았다.

## 직접 확인한 결과

| 사례 | 실제 결과 | 판정 |
|---|---|---|
| 12개 선택과 항목 추적 | 선택 12개 모두 고유 업로드 ID로 큐에 남고 앞 항목 실패 후 뒤 항목 전송 계속. 최종 12개 모두 저장 | PASS |
| 첫 청크 실패 | 세 번째 파일의 첫 PUT만 브라우저에서 503으로 모의. 같은 항목 재시도 후 같은 업로드 ID·사진 UUID로 저장 | PASS |
| 저장 후 응답 유실 | 다섯 번째 파일의 complete를 실제 서버에 전달해 200/저장 완료를 확인한 뒤 응답을 끊고 다음 조회도 503으로 모의. UI 실패 표시 후 새로고침·재조회로 이미 저장된 동일 UUID 회복 | PASS |
| 큰 사진 중간 실패 | 첫 4,194,304bytes 수신 후 다음 청크를 503으로 모의. 서버 offset 유지, 새로고침 후 원래 파일 재선택 안내 | PASS |
| 잘못된 파일 재선택 | 큰 사진 대신 크기가 다른 demo를 선택하면 거절. 서버 세션의 전체 응답 불변. 같은 크기의 다른 SHA 사례는 미실행 | PASS: 실행한 크기 불일치 사례 |
| 이어서 전송 | 원래 큰 파일을 다시 선택해 offset 4,194,304부터 나머지 여섯 청크 전송. 처음부터 재전송하지 않고 기존 업로드 ID·예정 사진 UUID 유지 | PASS |
| 13개 원본 보존 | 입력 파일·DB 메타데이터·API 원본 다운로드·MinIO 객체 직접 읽기의 SHA-256 및 크기가 13개 각각 일치. DB 행은 UUID당 한 개 | PASS |
| 실제 모델 완료 | 13개 모두 `done`과 동결 모델·전처리 버전 확인. demo 복제본 12개는 AI4, 합성 PNG는 AI2. 합성 PNG를 브라우저에서도 3072×3072로 디코딩·표시 | PASS: 처리 연결 |
| 중복 완료 요청 | 응답 유실 파일과 큰 파일 각각에 완료 POST 두 개를 동시에 재전송. 전부 200/동일 UUID, 세션·이력 전체 응답 불변. 각 이력 3→3개 | PASS |
| 같은 내용의 별도 신규 등록 | 13번째 파일은 기존 12개 UUID를 보존하며 새 사진 UUID 생성. 해당 항목의 모의 실패 후 재시도도 같은 신규 UUID 유지 | PASS |
| 모바일·태블릿·데스크톱 | 390×844, 768×1024, 1366×900, 1440×1000에서 선택·실패 큐와 필터 확인. 모달·문서 가로 넘침 없음. 긴 이름과 오류가 줄바꿈되며 파일 선택·재시도 버튼 접근 가능 | PASS: 해당 화면 |
| 필터 조회 수 가독성 | 네 화면 폭에서 `1장`·`미조회` 한 줄 표시, 문서 가로 넘침 없음. 이전 단위에서 관찰한 세로 줄바꿈 개선 확인 | PASS |

13개 모두 등록·판독 시작·판독 완료 이력이 각각 한 번씩 총 세 개다. 저장 성공과 AI 완료를 별도로 조회했다. 실패 응답은 지정한 브라우저 요청에만 적용했으며 마지막 finally까지 모든 임시 route를 해제했다. DB·MinIO 직접 검사는 읽기 전용이었다.

390px 실패 큐의 원래 파일 선택 input은 행 좌우 경계 안에 있었다. 해당 행 client/scroll 폭은 332/332px, 모달은 370/370px였다. 이름·안내 문구는 여러 줄이지만 버튼 잘림이나 겹침은 없었다. 이 결과를 제품 전체 화면의 가독성 완료로 확대하지 않는다.

허브의 후속 화면 검토에서 두 가지 좁은 개선을 제품 담당에게 전달했다. 저장 큐가 최초 응답을 유지해 실제 AI 완료 뒤에도 대기·진행 안내가 남는 점과 390px 파일 선택 문구의 단어 분절이다. 후속 웹 후보에서 아래 두 경로를 재검증했다.

### 안내·재선택 화면의 좁은 후속 검증

2026-09-21 23:19 KST, 웹 BUILD `VKN6YkBI0RQQdShLOqjKj`, page SHA `034afd1e6cdc2ccf0c1323ab9b34bf2a73e0ada10a8f3bf9af6e3acf68cceb4a`, CSS SHA `60dadfa613cac358117ead8ac5b77d9dadcccecf1324c1d0db9e5896283c5240`를 인계값과 직접 대조했다. API r3·모델은 제품 담당이 유지했다.

- 기존 저장 큐 13개 모두 `원본 저장 완료 · AI 판독 결과는 사진 목록에서 확인하세요.`로 표시됐다. 초기 AI 응답을 현재 처리 상태처럼 안내하지 않는다.
- 기존 한 업로드의 GET만 receiving으로 모의해 390px 재선택 화면을 열었다. 파일 선택 문구는 한 줄이고 input이 버튼 경계 안에 있으며 키보드 포커스가 가능했다. 행 폭 332/332px, 모달 폭 370/370px로 가로 넘침이 없었다.
- 기존 파일을 `setInputFiles`로 다시 지정하면 같은 큐 항목이 전송 대기로 바뀌었다. 전송 버튼을 누르지 않았으며 브라우저의 쓰기 요청은 0개다. 모의를 해제하고 실제 세션을 다시 조회해 기존 13개가 원래 UUID의 저장 완료 상태로 돌아온 것을 확인했다.

위 세 조건은 PASS다. 화면 97~99를 직접 열어 확인하고 임시 route를 모두 해제했다. 13개 업로드·추론은 반복하지 않았으며 OS 네이티브 파일 선택창 미검증 경계는 그대로다.

## 중단한 자동화와 미검증 경계

긴 파일명 fixture를 복사할 때 첫 상대 경로가 잘못되어 파일 선택 전에 중단됐다. 절대 경로로 바로잡은 뒤 실행했으며 실패 로그도 남겼다. 필터 오류 검사의 첫 시도는 이전 성공 조회가 남은 상태에서 미조회 안내를 기다려 시간 초과됐다. 화면은 마지막 조회 수와 오류를 표시했다. 새로고침으로 첫 조회 조건을 만든 뒤 500 응답의 미조회 표시를 검증했다. 두 중단을 제품 결함이나 성공 실행으로 세지 않았다.

파일 선택은 `setInputFiles`로 검증했다. OS 네이티브 선택창·선택 취소 동작은 미검증이다. 13개와 위 바이트 크기의 성공은 무제한 수량·용량, 모든 브라우저, 모든 단절·만료·자원 부족 조건의 보장이 아니다. 24시간 만료·동일 크기 오파일·r3의 동시 수신 제한·서비스 재시작은 이번 독립 실행에 포함하지 않았다. 사용자의 실제 조작 시간과 v2 대표 흐름 시간도 측정하지 않았다.

기존 demo와 같은 원본·AI4·모델/전처리 버전을 확인했지만 전처리 내부 텐서의 변경 전후 비교를 직접 수행하지 않았다. 합성 PNG의 AI2는 전송·저장·추론 연결 증거이며 부식 등급의 정답이나 모델 정확도 증거가 아니다. 동결 모델의 기존 최종 test 60%/목표70% 미달은 그대로 유지한다.

## 증거와 인계

UUID별 입력/DB/API/MinIO 크기·SHA, 업로드 ID, 실제 AI 결과·버전, 이력 개수, 전체 요청·응답과 후보 소스 해시를 로컬 인계 JSON에 묶었다. 화면 71~96 총 26개를 직접 열어 확인했다. 경로는 Git 제외 `records/validation-browser-2026-09-21.local.md`에 기록한다. 공개 기록에는 개별 비공개 ID·내부 원자료 목록·DB 내용·인증값·스크린샷을 포함하지 않는다.

V2-08·V2-09는 위 후보·입력 범위에서 완료, V2-10은 명시한 실패·복구·중복 방지 사례 완료로 판정한다. V2-01과 V2-19는 부분 확인, V2-11의 OS 선택창과 그 밖의 후속 v2 기능은 미검증이다. 공유 배포의 동작 검증과 r3 부하 검사는 위에 명시한 담당·범위로 구분한다.
