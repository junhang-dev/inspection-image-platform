# v3 후보 B ROI·SHAP 변경 영향 검증

2026-09-22 KST. [후보 A 핵심 흐름](validation-v3-core-a-2026-09-22.md) 이후 수정된 ROI 화면·결과 경합과 보존을 좁게 검증했다. **아래 직접 검사 범위 PASS, 출시를 막는 결함은 관측하지 않았다.** 실제 모델 설명의 생성·서비스 재시작은 제품 담당 실행이며 QA는 저장 결과·현재 UI/API/DB를 독립 대조했다. 공유 최종 배포본의 전환·외부 쓰기·고정 주소 판정은 별도다.

## 후보와 경계

- 소스 manifest: `64cdaf9df670bb825b330afcbb78fc205ee74ae4900d360d891f4612b3703216`, 시작/종료 파일49개의 실제 SHA 일치.
- 웹 BUILD `5eKvJ5aXQvGi9HnWKig0o`. A와 분리된 웹/API3201/4501, MySQL/MinIO13311/19008.
- 픽셀 허용 대상을 승인 demo 복제본 한 사진으로 제한했다. 다른 사진의 image/thumbnail 요청은 브라우저에서 차단했고 새 analysis POST도 차단했다. 가드 때문에 발생한 이미지 요청 실패를 제품 결함으로 집계하지 않았다.
- 승인 사진 `3e1d29b1-d47b-49c4-8212-1cc20c9a71bb`, 기존 ROI `34bae503-6e01-47a8-bfcc-9f3f273e2816`, 기존 설명 작업 `e7b4d928-70b8-4ee9-8cb0-69bbc23677a2`를 사용했다.
- QA는 새 ROI `e296b519-ea6e-4f7b-8cfc-9ea927760bd3` 1개만 추가했고 해당 ROI의 수정/삭제/복원과 승인 사진의 숨김/복원을 수행했다. 회사 원본300·고정 test 픽셀 열람, 신규 전체/ROI 추론, 신규 SHAP 계산은0이다. 최종 DB의 analysis_job 행은 QA 전과 동일했다.

## 실제 UI와 API 결과

| 변경 영향 | 실제 관찰 | 판정 |
|---|---|---|
| ROI 선택이 사진 판단을 오염시키던 문제 | 기존 ROI 선택, SHAP 표시 off/on, 강도5→20 후에도 ‘보수 기록 열기’ enabled. ROI 저장 후에도 enabled | PASS |
| SHAP 표시 강도 | 실제 canvas alpha 합이 2,254,401→6,279,542로 변하고 off/on에서 canvas0→1. 저장된 작업 JSON·등급·잔차·기여도 불변. 스크린샷 직접 확인 | PASS |
| ROI/설명 기하 연결 | 실제 bbox와 방향 적용 크기로 계산한 canvas 위치/크기와 화면 관측 오차 최대0.008px 미만. 승인 사진의 ROI 한 사례에 한정 | PASS |
| 늦은 결과의 다른 선택 덮어쓰기 | 완료 작업 GET 응답을 지연시킨 채 기존 ROI→전체/새 영역으로 변경. 이전 결과 도착 후에도 선택값 빈 값, 결과 패널0/canvas0 | PASS |
| 저장 중 입력 보존 | UI 드래그로20/20/40/40% 영역 생성. POST를 지연시킨 동안 모든 ROI 컨트롤 disabled. 추가 드래그에도 좌표 불변, 응답 후 저장201 | PASS |
| ROI CRUD | 새 QA ROI의 너비40→35%, 이름 변경·삭제·복원 모두200. 같은 ID, version0→1→2→3. 삭제 시 선택 박스0, 복원 시1 | PASS |
| ROI 이력 | 기본 접힘, 펼친 뒤 생성/수정/삭제/복원의 사유와 좌표 이력 조회. 원본 사진 editVersion은 ROI 작업으로 변하지 않음 | PASS |
| 숨김 뒤 늦은 결과 | 기존 결과 GET을 지연시킨 뒤 승인 사진을 API로 숨김. 응답이 나중에 도착해도 ROI 패널/canvas0, 숨김 안내 표시. 결과 상세 응답에 결과 본문 없음 | PASS |
| 사진 복원 | 같은 사진 복원 후 기존 설명 isCurrent=true/canvas1. 전체 AI·사람 등급 불변. 기존 ROI와 설명 작업은 수정하지 않음 | PASS |

저장된 실제 SHAP을 표시했고 가짜 열지도를 생성하지 않았다. 표시 강도는 시각적 진하기이며 기여도 값이나 예측 점수를 조작하지 않는다. 사람이 확인한 부식 위치 정확도·설비 안전성·설명 타당성으로 확대하지 않는다.

## 수치와 응답 검증

저장된 실제 결과를 API에서 읽어 224×224의 유한 기여도 **50,176개**를 합산했다.

- 기여도 합 `7.9321428955167255`, 저장 합 `7.932142895516724`.
- 출력−기준값−합의 재계산 잔차 `0.2778571426302472`, 저장 잔차 `0.27785714263024897`, 허용값 `2.052500009536743`.
- 최대 절대 기여도와 mapScale 모두 `0.3811897458397599`. completed/cacheable 판정 일치.
- 실제 후보 B의 `verifyAnalysisResult`에 저장 결과를 넣어 수용 확인. 별도 합성 응답에서 **음수 잔차의 절댓값이 허용치를 넘는데 passed로 표시한 경우 거절**, residual_high/cacheable=false이면 수용, map 합과 보고 값이 어긋나면 거절했다. 이 합성 계약 검사는 새 모델 실행이 아니다.

서버 최종 저장 실패 후 작업 복구에 관한 제품 담당의 HTTP/단위 검사와 코드 수정을 인계받았다. QA가 DB 장애를 새로 주입하거나 해당 복구를 독립 재현하지는 않았다.

## 원본·이력·재시작 증거 대조

제품 담당이 저장한 재시작 전후 원시 스냅샷을 QA가 내용으로 비교했다. **8개 테이블의 행 배열과 객체 목록304개가 모두 동일**했다. entities325/history412/alias5/parent16/registry300/storage profile1/chunk4/session5였다. 재시작은 QA가 다시 실행하지 않았다. 객체 목록304를 원본 사진304장과 혼동하지 않는다.

그 후 QA CRUD가 끝난 시점의 실제 DB를 읽어 원래 source snapshot 행의 포함과 내용 일치를 확인했다.

| 원래 자료 | 실제 DB에서 내용 동일한 행 |
|---|---:|
| entities | 316 / 316 |
| history | 374 / 374 |
| 원본 registry | 300 / 300 |
| demo alias | 5 / 5 |
| 원본 parent 관계 | 16 / 16 |
| storage profile | 1 / 1 |

QA 후 entities326/history418은 새 QA ROI1과 ROI이력4·사진 숨김/복원 이력2의 증가와 일치했다. 원래 사진300의 메타데이터·기존 이력을 새 QA 자료로 덮어쓰지 않았다. 타임스탬프를 JS Date로 읽은 최초 비교는 문자열 형식 차이로 실패했으며, 원래 스냅샷과 같은 DB 날짜 문자열로 읽어 재검증했다. 데이터는 수정하지 않았다.

승인 사진 한 건의 실제 원본 API와 MinIO 객체는 모두 **2,965,056 bytes**, SHA `0510d0827704d8ec297bdecdebfd2eb7f65c44521e662fa8532036e47a610838`로 입력 demo와 같았다. 원본299개 객체 전수 바이트 해시는 제품 담당 복제 영수증의 검사이며 QA가 전체 원본을 다시 다운로드한 것은 아니다.

핵심 증거 식별자는 다음과 같다. 상세 파일과 DB/객체 경로는 로컬 전용이다.

- 제품 source snapshot SHA `df27a3a487992d4f530355e70c70e23258e40617a3c2b80210f4c0c33ecec2fa`.
- 제품 재시작 전/후 SHA `378c46d6bbb38ac7fac3f60b3d4a4bd9aa0903def957013c58a455a7fa649c5b` / `0ab202b61a313082ea1601932272f46ae00a59c1350265cf13ca1e1496dc8d62`.
- QA 로컬 증거22개의 파일별 SHA manifest: `f7cab6c99a68404b3b48e082a0f0af4413e6fb52e4ef2123e31016edeee0a8d2`.

## 최종 인수에 남기는 구분

후보 A의 핵심 업무와 B의 변경 영향 검증을 인수할 수 있다. 이 판정은 모델 정확도 개선, 새 독립 최종 평가, 사람의 전체 업무시간/동료 검수, 최종 공개 프로필의 전환 및 고정 주소 재기동까지 자동으로 통과시키지 않는다. 기존 성능 FAIL은 유지한다. 원본 공개에 대한 후속 사용자 승인이 있어도 Git에 원본·DB·가중치·비밀을 게시하지 않으며 고정 test의 새 추론/ROI/튜닝 금지를 유지한다.
