# API 업무흐름 독립 검토 — 2026-09-21

- 검토 시각: 2026-09-21 16:46~16:48 KST.
- 방식: 구현 담당의 작업 중 `server/index.mjs`, `server/store.mjs`를 읽기 전용으로 확인. 코드 변경·API 호출·브라우저 조작·DB 변경은 하지 않음.
- 검토 대상은 미커밋 작업 파일이며 최종 버전의 검토 결과가 아님. 16:48 시점 SHA-256: index `f6bfe5ea11f05ad6d3af6acb9582c8b1998f87e370bfd34a7ac5a4adde6bd869`, store `8b961673cfde7e6822cf1089ecd1b4cc02cd3c6fabb1a8ec4fbc649246b907c0`.
- 범위: 포인트 상태·이력·검사 계획·TA 업무흐름. 담당의 별도 API 기술 검토와 중복되는 세부 보안 검토는 수행하지 않음.

## 수정 요청

**계획 완료·취소의 변경 이력을 재조회할 경로가 없음.** 계획 변경은 `update('plan', ...)`에서 전후값·수정자·시각·사유를 이력에 저장하지만, API에는 계획 목록·등록·상태 수정만 있으며 계획 이력 조회 경로가 없다. 계획 상태를 바꾼 후 그 사유와 이전 상태를 다시 확인할 수 있어야 하므로 계획별 이력 API와 화면 접근 경로가 필요하다. F01/F10/F12 관련이며 구현 담당과 허브에 전달했다. 수정 및 실제 실행 확인은 대기 중이다.

## 화면 구현 시 확인

포인트 이력 API는 포인트 자체의 entity 이력만 반환한다. 연결된 사진의 등급 수정·재촬영과 계획 상태 변경은 각각 다른 entity 이력이다. 포인트 상세에서 연결된 사진/계획의 개별 이력으로 접근할 수 있는지 실제 화면에서 확인한다. 이력을 반드시 하나로 합쳐야 한다는 새 요구는 아니다.

## 코드상 확인한 설계

- 포인트의 `repairStatus`, `managed`, `ta`와 사진의 `retake`, `labeling`, `humanGrade`가 분리돼 있다.
- 수정값과 이력 전후값은 동일 DB 트랜잭션에 저장한다.
- 사람 수정 스키마가 원래 AI 결과 필드를 직접 받지 않으며, 원래 AI 결과와 사람 등급이 별도 필드다.

위 내용은 코드 독해 결과다. 실제 상태 보존·재시작·UI 접근·동시 조작·시간 성능이 통과했다는 뜻은 아니다. 서비스 기동 알림 후 실제 검수한다.

## 후속 화면 확인

16:54~16:57 KST 실제 브라우저로 대시보드와 사진 미선택 오류, 계획 입력 폼, 모바일 화면을 확인하고 스크린샷을 저장했다. 초기 HTTP 404는 이후 대시보드 렌더링으로 해소됐다. API는 아직 연결되지 않아 사진 업로드·저장·재조회는 미검증이다. 세부 화면과 도구 증거 경로는 Git 제외 `validation-browser-2026-09-21.local.md`에 기록했다. 이 자동 브라우저 확인은 실제 비개발자 팀원 사용자 검수가 아니다.

17:05 KST 수정된 계획 이력 API와 화면에서 계획 등록→완료 전후 상태·수정자·시각·사유를 직접 재조회해 위 수정 요청의 해결을 확인했다. 후속 실제 업로드·파일·이력·재시작 검증은 `validation-runtime-2026-09-21.md`에 기록한다.
