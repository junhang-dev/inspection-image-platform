# 모델 초기 연결 실행 기록

- 실행일: 2026-09-21 KST. 실제 작업 시작 약 16:34, 첫 모델 생성 16:40:36.
- 시작 커밋: `46f8cd23b6032741d08310b0108b205df5ae9787`.
- 브랜치: `codex/jun/model-bootstrap`. 담당 경로: `ml/`, `records/model-*`.
- 목표: 기존 고정 train/validation으로 작은 실험을 수행하고 픽셀 기반 실제 추론 API를 제품에 연결할 수 있게 준비한다.
- 사용자 최신 변경에 따라 실측 두께 관련 기능·평가는 이번 범위에서 제외한다.
- 실제 작업 지시와 비공개 자료 위치·상세 결과는 Git 제외 로컬 기록에 보존한다.

## 확인한 등급 기준

제공된 `ref/등급판정기준.png`를 직접 확인했다.
1. 부식 형상 없음.
2. 부식 형상이 있지만 스케일 거의 없음.
3. 일부 구간 단일층 부식 스케일 형성.
4. 일부 구간 다층 부식 스케일 형성.
5. 전구간 다층 부식 스케일 형성.

이 기준의 시각적 분류다. 기존 라벨의 전수 현업 재판정이나 설비 안전성을 검증하지 않았다.

## 실제 실행과 모델

- 프로젝트 `ml/.venv` 설치 성공: Python 3.11.6, torch 2.14.0, torchvision 0.29.0. 전체 버전은 `ml/requirements.txt`.
- 공개 ImageNet MobileNetV3 Small 가중치를 다운로드했고 원본 사진 외부 전송은 수행하지 않았다. 출처는 `ml/README.md`.
- 기존 manifest의 train 231장/validation 58장만 읽었다. 입력마다 SHA-256 일치 확인. 고정 test 10장의 사진은 열지 않았다.
- train/validation/test 개수·ID·등급·기존 그룹/해시 겹침은 manifest 메타데이터로 추가 확인한다. test 사진 내용은 접근하지 않는다.
- 첫 프레임/EXIF/RGB/전체 resize224/ImageNet 정규화를 학습·추론에서 동일하게 사용한다.
- 고정 특징 추출 + 선형 분류기 10개 조합을 실행했다. validation macro F1, 동률이면 정확도로 선택했다. 이는 10회 연속 정체 판정이 아니다.
- 모델: `mobilenetv3small-linear-20260921T074036Z`.
- 전처리: `rgb-first-frame-exif-full-resize224-imagenet-v1`.
- 장치: CPU 2 threads. 현재 실행 환경에서 MPS available=false. 학습·검증 44.01초, 프로세스 peak RSS 약 777MB. Mac 전체 메모리는 샌드박스 제한으로 조회하지 못했다.

## validation 58장 결과

| 지표 | 실제 결과 |
|---|---:|
| 정확한 5등급 일치 | 32/58, 55.17% |
| macro F1 | 0.4871 |
| balanced accuracy | 52.68% |
| 1·2 / 3~5 구분 정확도 | 46/58, 79.31% |
| 실제 3~5등급을 3~5로 판독한 재현율 | 14/21, 66.67% |
| 4등급 재현율 | 0/2 |
| 5등급 재현율 | 4/4 |
| 실제 4·5등급을 1·2로 크게 낮춘 건수 | 0/6 |

4·5등급 표본이 적고 특정 촬영 묶음에 집중돼 있다. validation은 모델 선택에 사용했으므로 독립 최종 성능이 아니다. confidence는 보정되지 않은 softmax 점수다.

초기 팀 판단을 위해 제안했던 **미승인 권장안(이후 채택되지 않음)**: 최종 test 정확 일치 7/10 이상, 3~5 재현율 5/6 이상, 실제 4·5를 1·2로 판독하는 경우 0건. test가 10장뿐이므로 기준을 통과해도 현장 일반화나 안전성을 입증하지 못한다. 최종 모델·전처리를 고정하고 사람이 합격선을 확정하기 전 test를 실행하지 않는다.

## 실제 기능 검증

- `python -m unittest ml.test_service -v`: 4개 테스트 통과. 실제 가중치 추론, 같은 bytes의 파일명 변경 불변성, 손상/없음/지원형식/모델미준비/파일20MiB초과/원시본문21MiB초과 포함.
- 독립 기술 검토에서 multipart 파싱 전 수신량 제한 부재 1건 지적. ASGI 본문 제한을 추가했고 재검토에서 중요 결함 없음 보고.
- 실제 Uvicorn 프로세스의 `/health` ready=true, HTTP `/predict` 정상 응답 확인.
- 승인된 demo 5장을 두 번 요청한 10회 직렬 HTTP 추론: 총 0.968초. 1회 최대 0.225초. 로컬 CPU/loopback 측정이다. demo는 train 복제본이며 별도 학습 표본이나 평가 세트로 집계하지 않았다.
- 저장된 모델로 validation 58장을 다시 추론하여 학습 리포트의 예측과 58/58 일치 확인(4.40초). test 사진은 읽지 않았다.
- 모델 가중치, pretrained cache, 상세 리포트, 가상환경 Git 제외 확인.

## 완료 경계

- 모델 API 기능: 로컬 실행·검증 완료. 제품 전체 백그라운드 처리/배포/재시작 보존은 제품 담당 통합 검증 대상.
- 모델 성능: 첫 validation만 확인. 합격선 미확정, 최종 test 미실행. 성능 완료가 아니다.
- 사용자 전체 흐름 검수: 아직 미검증.
- 통합/cherry-pick/push: 이 담당 작업에서 수행하지 않는다. 허브와 Git 작성 슬롯을 조율한 뒤 담당 변경만 커밋한다.

## 합격선 후속 확정

2026-09-21 약16:51 KST 허브를 통해 실제 사용자 지시를 받았다: train exact accuracy 90% 이상, test exact accuracy 70% 이상. 위의 초기 권장 recall/심각저평가 조건은 합격 gate에 추가하지 않고 참고 지표로만 보고한다. train90 달성을 위해 validation이 나빠진 후보를 선택하지 않는다. 최종 모델·전처리·가중치 해시를 동결한 후 test를 한 번 평가하고 같은 모델의 제품 연결을 확인한다.
