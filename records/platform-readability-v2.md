# 글자 크기와 화면 간격 조정

- 시작 제품 커밋: `0159062f66a3064b83fe7a3139e463a7790caab2`.
- 작업일: 2026-09-21 KST.
- 변경: `src/app/globals.css`의 본문/입력/주요 버튼 16px, 표/탐색 15px, 보조 문구 13px 이상, 제목 28~32px/22px, KPI 32~40px. 작은 화면에서 다시 과도하게 축소되던 규칙도 조정했다.
- 연한 녹색 보조 문구·배지·placeholder 대비를 높였다. 독립 담당자가 점검한 주요 조합의 계산상 대비는 4.85~5.98:1이다. 모든 화면의 접근성 인증 결과는 아니다.
- 필터 건수는 최소 너비와 줄바꿈 금지로 한 줄을 유지한다. 모달·긴 선택값·계획 상세에는 줄바꿈과 작은 화면의 1열 배치를 적용했다. 파일별 업로드 큐의 카드·작업 버튼 배치도 추가했다.

독립 담당자는 CSS만 편집하고 Prettier, diff 공백 검사, 명시 폰트 크기 최소 13px/최대 40px, zoom 규칙 없음, 임시 미리보기 HTTP 200을 확인했다. 독립 미리보기의 최초 dev 실행은 파일 감시 오류로 중단했으며 전역 설정을 바꾸지 않았다. 해당 담당자의 브라우저 검증은 실행 제약으로 미완료였고 임시 프로세스는 종료했다.

제품 후보 `IH4Hic5nFa25IVgE76ubE`에서 QA가 390/768/1366/1440px의 선택·실패 큐, 긴 이름, 모달과 필터 건수를 실제 확인했다. `1장`/`미조회`는 한 줄이며 문서·모달의 가로 넘침과 버튼 잘림은 없었다. 전체 제품의 모든 화면에 대한 접근성 인증은 아니다.

후속 검토에서 390px 재선택 라벨이 단어 중간에서 줄바꿈되는 점을 보완했다. 메인 파일 선택과 같은 접근 가능한 투명 input을 라벨 버튼 전체에 배치하고 라벨은 한 줄을 유지한다. 최종 후보 `VKN6YkBI0RQQdShLOqjKj`에서 QA가 390px 라벨 한 줄 표시·파일 재선택·키보드 포커스를 확인했다. 추가 사진 전송은 없었다. 공개 빌드 `qcsQTq-_XDX_JXq_4Qn9M`에 반영했다. API·원본·DB·모델에는 이 CSS 변경만으로 영향을 주지 않는다.
