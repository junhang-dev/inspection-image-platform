# PlantPilot v2 공통 조회·숨김·지도 독립 QA

## 실행 범위와 버전

2026-09-21 23:52 이후부터 9월 22일에 걸쳐 격리 웹 3500/API 3400에서 실행했다. 첫 후보는 기반 제품 커밋 `e440bf3a2fcc017b9eef5464715d3c4e0a131335`, 웹 BUILD `IyuqkJS2IEnmlYmEqTONK`, API 이미지 `sha256:29eda584989b9cbb6fa5d6bfedf962f9a5e9681772db8904272b0348ab149aa3`다. 인계 manifest의 소스·지도 자산 11개 해시와 실제 후보 파일, BUILD_ID를 직접 대조했다.

제품 담당이 준비한 합성 메타데이터를 사용했다. 시작 상태는 사진 1,042개·포인트 32개·계획 12개다. 이 중 1,001개는 페이지 검사용이며 KPI 기준 사진 여섯 개와 별도 계획·미지정·목적·숨김 사례가 포함된다. 사진 원본은 기존 승인 demo 객체를 참조한다. 격리 AI worker는 합성 상태가 변하지 않도록 제품 담당이 의도적으로 연결 대기 상태로 두었다. 합성 AI 값과 처리 상태를 실제 추론·모델 정확도·처리 속도의 증거로 사용하지 않았다. 공유 운영 서비스·회사 원본·고정 test는 조작하지 않았다.

## 직접 실행한 결과

| 사례 | 실제 결과 | 판정 |
|---|---|---|
| 네 KPI의 키보드 이동 | 같은 계획·팀·랙·문자 검색 조건에서 전체/보수/대기·진행/재촬영 카드를 각각 Enter로 실행. 목록 필터와 수량 6/2/2/2, 응답 UUID와 화면 행이 일치하고 공통 조건 유지 | PASS |
| 사람 등급 우선·집합 중첩 | AI5/사람2는 보수 분류에서 제외, AI2/사람4와 pending/사람4는 포함. pending·processing 두 상태가 대기·진행 카드에 포함 | PASS |
| 계획 분리·문자 검색 | 같은 포인트의 계획 B와 계획 미지정 사진이 자기 조건에서만 조회. `%_=` 검색을 포함한 UI와 API의 실제 ID 대조 | PASS: 해당 자료 |
| 1,001개 목록 | UI 전체 1,001장/21페이지. 마지막 페이지 한 장에서 단건 상세를 열어 동일 UUID 이미지 경로 확인 | PASS |
| 계획·포인트 상세 전체 수 | 각 상세가 전체 1,001장을 표시하고 마지막 페이지에서 메인 목록과 같은 사진 ID 접근 | PASS |
| 최근 사진 숨김·복원 | 합성 사진 한 장을 최근 목록에서 열어 숨김. 최근 목록에서 제외, KPI 전체 6→5/보수 2→1/재촬영 2→1, 지도 포인트 사진 수 6→5. 숨김 필터·상세에서 찾아 복원 | PASS |
| 숨김 원본 접근과 보존 | 숨긴 사진의 이미지·썸네일 404, 상세는 숨김 안내. 복원 전후 API 원본과 직접 읽은 MinIO 객체의 바이트 크기·SHA 일치. 원래 AI·사람 판단·관계 유지 | PASS |
| 사진 목적 변경 | 같은 합성 사진을 inspection→verification으로 바꾸면 기본 최근·KPI·지도 수에서 제외되고 검증용 조건에서 조회. inspection으로 복귀 후 기준 수량 회복 | PASS |
| 지도 등록·계획 연결 | 정유1팀/파이프랙1에서 새 QA 포인트 한 개를 UI 등록하고 선택한 QA 계획에 연결. 기존 포인트·기존 계획 연결 보존, 새 ID·가상 좌표 저장 및 새로고침 후 위치 유지 | PASS |
| 모바일 지도 | 320px·390px에서 안정된 화면을 직접 확인. 문서 가로 넘침 없음. 지도와 목록의 포인트 선택·상세 접근 확인 | PASS: 해당 화면 |
| 조회 실패 | 공통 query GET만 500으로 모의. KPI 네 개·목록·조건 의존 지도는 미조회로 안내하며 실패를 0건으로 표시하지 않음. 모의 해제 후 재조회 회복 | PASS |
| 메인·재선택 실제 클릭 | 각 파일 선택 영역을 실제 클릭해 Playwright CLI가 file chooser 이벤트를 보고. 승인 파일 지정 후 선택 이름·개수 또는 같은 큐 항목의 전송 대기 상태 반영 | PASS: 브라우저 이벤트·파일 입력 |

KPI 집합은 서로 겹친다. 첫 후보의 `summary`는 분류 필터를 제외한 공통 조건, 목록 `total`과 `pointCounts`는 현재 분류까지 포함한 조건이다. 기본 페이지 크기 50에서 전체 수와 현재 행 수를 구분해 확인했다. 페이지 API의 전 페이지 1,042개 ID도 읽어 고유 수량을 확인했으며 구 배열 endpoint의 1,000개 상한을 전체 수량으로 오인하지 않았다.

파일 선택은 브라우저가 발생시킨 file chooser 이벤트를 CLI로 처리했다. OS 네이티브 대화상자 화면을 직접 본 검증은 아니다. 두 선택 모두 전송하지 않았고 새 사진을 만들지 않았다. 재선택 검사는 기존 완료 세션 한 개의 로컬 큐를 임시 복원하고 GET만 receiving으로 모의했다. 종료 후 서버 세션 전체 응답이 전과 같음을 확인하고 모의·임시 로컬 큐를 해제했다.

## 실제 변경과 보존

합성 사진 한 개의 숨김→복원→목적 변경→목적 복귀로 editVersion 2→6, 이력 3→7개가 됐다. 최종 상태는 원래의 visible/inspection이며 전체 사진 1,042개 중 이 사진의 editVersion·updatedAt 외 응답 필드 변화는 없었다. 원본은 2,965,056bytes, SHA-256 `0510d0827704d8ec297bdecdebfd2eb7f65c44521e662fa8532036e47a610838`로 API 전후/DB 메타데이터/MinIO 직접 읽기가 일치했다.

지도 QA 포인트 한 개는 검증 증거로 보존했다. 기존 포인트 32개 전체 응답은 동일하고 최종 33개다. 계획 12개 중 대상 계획 한 개만 기존 pointIds에 새 포인트 ID를 추가하고 버전·갱신 시각이 바뀌었다. 다른 계획은 동일했다. 원본·이력 삭제나 사용자 기존 기록의 목적 재분류는 수행하지 않았다. 정확한 ID·최종 상태·전후 응답은 로컬 인계 자료에 남겼다.

## 발견한 결함과 자동화 보정

첫 화면이 뜨기 전 지도 이미지 요청을 abort하면 overview 이미지의 `complete=true`, `naturalWidth=0`인데 오류 안내가 표시되지 않는 결함을 재현했다. 이후 랙 이미지가 실패하면 안내가 정상 표시되고 목록에서 포인트 상세에 접근할 수 있었다. 실제 실패 요청 URL과 초기/후속 화면을 제품 담당에게 전달했다. 제품 담당이 초기 이미지 상태 확인을 추가한 뒤 아래 후속 후보에서 같은 실패를 재현해 수정 결과를 확인했다.

첫 숨김 필터 검사에서 기존 숨김 fixture 한 개를 기대 수에 포함하지 않아 1장을 기다리며 자동화가 중단됐다. 실제 화면의 두 개 ID를 확인하고 기대값을 바로잡아 같은 변경 대상을 이어서 복원했다. 포인트 단건 GET이 있다고 가정한 최초 기준 읽기도 404로 중단돼 목록에서 해당 ID를 선택하도록 수정했다. 320px의 첫 캡처는 리사이즈 직후 이미지가 완전히 그려지기 전이라, 디코딩·화면 안정 뒤 다시 캡처했다. 이 중단과 미완성 캡처를 제품 기능 실패 또는 성공 증거로 대체하지 않았다.

파일 선택 이벤트는 CLI가 별도 modal 상태로 반환하므로 하나의 run-code 반환값만으로 실행 완료를 판정하지 않았다. 실제 click 로그, file chooser 보고, 파일 지정 명령, 이후 화면과 세션 불변을 함께 보존했다.

## 후속 후보의 좁은 재검증

2026-09-22 00:13~00:22 KST, 웹 BUILD `je9CWa3dAYwz9DXtCVrWA`, API 이미지 `sha256:fa7cb63d165ae147b841e3015187e6e028c77275560b1802e2a6d0d3bbd9851c`의 인계 소스 해시·BUILD_ID를 직접 대조했다. 첫 후보와 같은 버전으로 합산하지 않았다. 제품 담당이 중간 API 재시작과 자체 검사를 수행했으며, 후속 QA는 사진 1,043개·포인트 34개·계획 13개의 새 기준을 읽고 시작했다. 기존 processing 합성 사진의 pending 재큐잉은 제품 담당의 재시작 결과로 구분한다.

| 추가 사례 | 실제 결과 | 판정 |
|---|---|---|
| 최초 지도 이미지 오류 | 첫 overview 요청부터 abort한 뒤에도 오류 안내와 목록 경로가 표시됨 | PASS |
| 검색 입력 크기 | 검색창 높이 46.796875px, 옆 선택창 44px. 기존 약 26px 기본 입력 문제 해소와 실제 화면 확인 | 개선 확인: 두 높이가 정확히 같지는 않음 |
| 사진 없는 포인트 목적 | QA가 만든 빈 포인트만 명시 CAS로 verification/presentation으로 바꿔 기본 업무 목록 제외, 해당 목적·all에서는 조회 완료 뒤 0장으로 표시. 최종 inspection으로 복귀 | PASS |
| 혼합 부모·자식 목적 | presentation 포인트에 연결된 verification 사진이 검증용 조건에서 포인트와 함께 조회됨. 해당 기존 자식의 전체 응답은 전후 동일 | PASS: 조회 유지 |
| 계획 목적 선택 | UI에서 verification을 선택해 QA 계획 한 개 생성. 실제 POST 목적과 저장값 일치, 기본 업무 달력에서 제외되고 verification 조건에서 접근. 기존 presentation 계획도 기본 제외/all 접근 | PASS |
| 업로드 목적 보존 | 승인 demo 한 장을 verification으로 전송. 첫 청크 503 후 새로고침·파일 재선택·이어서 전송. 새 입력 폼의 목적을 presentation으로 바꿔도 기존 큐·세션·저장 사진은 verification과 같은 UUID 유지 | PASS |
| 새 사진 원본 | 입력·DB·API 실제 다운로드·MinIO 직접 읽기의 2,965,056bytes와 승인 demo SHA 일치. 신규 사진은 정확히 한 개 | PASS: 저장·원본 |
| KPI 빠른 회귀 | 기존 계획의 네 카드가 6/2/2/2 유지 | PASS |

새 목적 업로드는 `pending`, AI 결과 없음이다. 의도적인 격리 worker 대기 상태에서 저장·복구·목적만 검증했으며 실제 판독 성공으로 표시하지 않았다. 기존 13개 대용량 업로드·모델 검사는 반복하지 않았다.

후속 최종 상태는 사진 1,044개·포인트 34개·계획 14개다. 시작 사진 1,043개와 계획 13개는 모두 전체 응답이 동일하고, 새 검증용 사진 한 개·검증용 계획 한 개만 추가됐다. 기존 포인트는 QA 소유 한 개의 editVersion·updatedAt만 변했다. 이 포인트의 목적은 inspection으로 복귀했으며 최종 version6/이력7개다. 조회 완료를 기다리지 않은 첫 표시 검사에서는 `미조회`를 읽어, 동일 포인트로 다시 확인한 이력도 모두 보존했다.

후속 자동화의 지도 DOM 교체 시점·검색 입력의 새 `searchbox` 역할·새로고침 후 두 업로드 버튼 구분을 보정했다. 마지막 중단 때 이미 생성된 전송 세션을 보존해 이어서 사용했으며 새 업로드를 중복 생성하지 않았다. 초기 `search44`의 정확한 높이 일치 비교는 false였고 실측값으로 보고했으며, 빈 포인트의 초기 미조회 비교도 최종 조회 완료 결과와 구분했다.

## 남은 경계와 증거

이 기록은 실행한 후보와 합성 자료의 기능 검증이다. 서비스 정상 재시작, 숨긴 processing 사진의 실제 늦은 모델 응답, 전체 v2 대표 흐름 시간, 현업 사용자 검수, 모든 가상 팀의 전체 경로, 모든 브라우저·화면 크기는 이번 직접 실행으로 완료하지 않았다. 모델 성능의 기존 미달 결과도 바뀌지 않는다.

부모 계획·포인트 목적과 신규 업로드 목적은 위 후속 실행 범위에서 확인했다. 부모의 목적 변경이 기존 자식 전체를 재분류하지 않는 API 경합 검사는 제품 담당의 별도 실행 결과이며, QA의 혼합 목적 조회 확인과 구분한다. 기존 자료 전체를 이름이나 자식 목적에 따라 자동 재분류하는 작업은 이 QA에 포함하지 않는다.

화면 100~116과 108a를 직접 열어 확인했으며 105의 리사이즈 직후 미완성 캡처는 107로 보정했다. 모든 임시 query·지도·업로드 세션 모의는 해제했고 모달을 닫은 뒤 제품 담당에게 서비스 슬롯을 반환했다. 상세 증거는 Git 제외 `records/validation-browser-2026-09-21.local.md`에 연결하며 공개 문서에 개별 비공개 ID·DB 내용·내부 경로·스크린샷을 포함하지 않는다.
