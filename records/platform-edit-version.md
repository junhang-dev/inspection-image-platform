# 동시 수정 충돌과 입력 보존

- 실행일: 2026-09-21.
- 시작 제품 커밋: `9dbb58e4f1f0e53a697474318b07357e26e7c696`.
- 범위: 같은 사진·포인트·계획을 여러 화면에서 수정할 때, 오래된 값으로 다른 사람의 변경을 덮어쓰는 문제. 모델·원본·등급 기준은 바꾸지 않는다.

## 변경한 동작

기존 행 잠금은 두 저장을 순서대로 실행하지만 두 번째 화면의 오래된 전체 폼을 거절하지 못했다. 사람의 업무 수정에는 `expectedVersion`을 받고, 같은 트랜잭션에서 잠근 최신 행의 `editVersion`과 비교한다. 같으면 업무 값과 버전 증가를 이력과 함께 커밋하고, 다르면 데이터·이력 쓰기 전에 HTTP 409로 거절한다.

- 기존 JSON에 버전이 없으면 0으로 읽는다. 신규 항목은 0으로 생성한다. 목록·단건 조회·시연 사진 재사용 응답 모두 같은 정규화를 사용하며 별도 테이블 변경은 없다.
- 사람의 수정이 성공할 때만 버전을 1 증가시킨다. AI 판독 상태·결과·오류는 서버 내부에서 명시한 경로로만 최신 행에 병합하고 사람 값과 수정 버전을 보존한다. 사용자 입력인 수정자 이름으로 이 예외를 판별하지 않는다.
- 사람 PATCH에서 버전 누락·음수·소수·문자열·직접 버전 주입·내부 옵션 주입을 거절한다. 이전 웹을 열어 둔 경우에는 새로고침 안내를 제공하고 저장을 막는다.
- 사진·포인트 상세 창은 처음 열린 값과 버전을 함께 보존한다. 3초 목록 갱신으로 AI 결과가 바뀌어도 작성 중인 폼의 기준 버전을 자동 변경하지 않는다. 충돌 시 입력을 유지하고 필요한 내용을 복사한 뒤 창을 닫고 새로고침하여 최신 내용을 확인하도록 안내한다.
- 계획의 완료 상태 변경에도 같은 버전 비교를 적용한다. 원래 AI 결과와 사람 판단은 별도 필드로 유지된다.

## 실제 검증

- 타입 검사·후보 프로덕션 빌드 통과. 단위/API 회귀 검사 17개를 통과한 뒤, 독립 검토가 발견한 기존 시연 사진 재사용 응답의 버전 누락을 수정하고 관련 단위 검사 1개를 추가했다. 수정 후 저장소 단위 검사 6개를 통과했다.
- 21:10 KST 공유 서비스와 분리된 실제 MySQL·MinIO·API에서 사진·포인트·계획 각각 같은 버전의 요청 2개를 동시에 전송했다. 세 종류 모두 200 한 건과 409 한 건, 버전 1 증가와 이력 1개 증가를 확인했다. 실패한 요청의 전체 폼이 승자의 값을 덮지 않았다.
- 같은 실제 항목에 수정자 `AI 서비스`로 오래된 버전을 보내도 409였다. 잘못된 버전·주입 6종은 모두 422였고 데이터와 이력이 변하지 않았다.
- 승인된 demo 사진 1장과 검증용 포인트·계획 각 1개만 격리 환경에 추가했다. 사진은 동결된 실제 모델로 판독했다. 이후 트랜잭션 병합 검사는 기존 실제 AI 결과를 같은 값으로 재사용한 내부 호출이며, 새 모델 추론 성능 검사로 간주하지 않는다. AI 갱신 → 사람 저장 → AI 갱신의 버전은 1 → 1 → 2 → 2였고 사람 판단·원래 AI 결과·원본 해시가 유지됐다.
- 21:13 KST 실제 DB에 버전이 없는 기존 시연 사진을 다시 요청했다. 새 사진을 만들지 않고 `existing` 응답의 버전 0으로 첫 PATCH가 성공해 버전 1·이력 1개가 추가됐다. 사진 수 11개와 기존 판단·AI·원본 해시는 그대로였다. 첫 검증 스크립트는 재사용의 정상 HTTP 200을 201로 잘못 기대하여 PATCH 전에 중단했으며, 기대값 수정 후 통과했다.

- 21:14 KST 후보 빌드 `mh45wA3upwFbJcp9wEu4_`의 실제 두 브라우저 창에서 사진·포인트를 각각 수정했다. A가 버전 0으로 저장하면 B의 버전 0은 409였다. 목록의 실제 주기 조회 응답에서 버전 1을 받은 후에도 B는 버전 0으로 재시도하여 다시 409였으며 사유·등급·체크 상태가 유지됐다. 닫고 새로고침하여 다시 연 B는 버전 1로 저장하여 성공했다.
- 이 화면 검사는 사진 이력 4 → 5 → 5 → 6, 포인트 이력 1 → 2 → 2 → 3과 버전 0 → 1 → 2를 확인했다. 거절 전후 행과 이력의 JSON이 같았고, 최신 포인트에서 A의 관리 대상 값과 B의 TA 값이 함께 보존됐다. 실제 저장 성공 4건·거절 4건이며 새 사진·새 모델 추론·공유 서비스 쓰기는 없었다. QA는 화면 캡처 6장을 직접 확인했다.

## 공유 반영

- 기존 시연 재사용 응답의 버전 누락 수정 후 최종 독립 재검토에서 차단 결함이 없음을 확인했다.
- 21:20 KST 같은 소스로 빌드한 운영 웹 `c7PYJmb11EHoAhO6I-daY`와 API를 함께 교체했다. 별도 임시 소스의 웹·API 파일은 QA 후보 및 작업 소스와 해시가 같으며 기존 환경 설정을 참조한다. 이전 웹·API 아티팩트는 함께 복구할 수 있도록 보존했다.
- 로컬 3000 및 외부 공유 URL 모두 새 HTML 빌드, 루트·건강 검사 HTTP 200, API·모델 준비 상태와 버전 포함 응답을 확인했다. 외부 브라우저의 검사 계획 화면도 정상 연결 상태로 표시됐다.
- API 교체 전후 사진 64개·포인트 4개·계획 4개의 전체 엔티티 JSON을 비교했다. 새 응답 필드 `editVersion: 0`을 제외한 기존 값이 모두 같았다. 원본 해시 필드·AI 결과·사람 판단을 포함한 메타데이터 비교이며 사진 64개를 다시 다운로드한 검사는 아니다. 공개 서비스에 검증용 저장 요청은 보내지 않았다.
- API 재기동 중의 짧은 연결 중단 후 정상 복구됐다. DB·MinIO·동결 모델·터널은 그대로 유지했고 후보 웹과 별도 운영 미리보기는 종료했다. 기존 브라우저 화면은 새로고침해야 새 저장 계약을 사용한다.

## 한계

충돌한 두 사람의 변경을 자동 병합하지 않는다. 최신 내용을 확인하여 필요한 판단을 다시 저장해야 한다. 사진 전체 부식 분류 모델의 최종 test 정확도 60%, 목표 70% 미달 결과는 이번 저장 정합성 수정과 별도로 유지한다.
