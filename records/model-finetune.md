# 작은 미세조정 실행 결과

2026-09-21 16:46~16:49 KST, 초기 코드 커밋 `58ee243`에 이어 허브가 허용한 validation 개선 검토를 수행했다. train 231장 / validation 58장을 그대로 사용했고 고정 test 사진은 읽지 않았다. 실험 가중치·개별 결과는 로컬에만 보관한다.

## 실제 변경과 실행

- 초기 MobileNetV3 Small의 마지막 feature block 3개와 선형 분류기만 학습했다. 초기 모델 가중치는 그대로 보존했다.
- BatchNorm 통계 고정, train 좌우반전, 등급빈도의 역수 제곱근 가중치, AdamW를 사용했다. 학습률은 backbone 0.0001 / head 0.0003.
- CPU 2 threads에서 12epoch 실행, 약 114.97초, peak RSS 990.94MB. 초기 epoch 0도 후보에 포함했다.
- 선택 기준은 기존과 동일한 validation macro F1 우선 / 동률 정확도. epoch 11을 선택했다. 마지막 개선 뒤 1epoch만 경과했으므로 '10회 연속 개선 정체'에 도달하지 않았다.
- 후보 모델 `mobilenetv3small-ft-20260921T074822Z`, 전처리는 기존 `rgb-first-frame-exif-full-resize224-imagenet-v1`.

## validation 결과 비교

| 지표 | 연결 중인 초기 모델 | 미세조정 후보 |
|---|---:|---:|
| 5등급 정확 일치 | 32/58, 55.17% | 37/58, 63.79% |
| macro F1 | 0.4871 | 0.5561 |
| 1·2 / 3~5 구분 정확도 | 46/58, 79.31% | 45/58, 77.59% |
| 실제 3~5등급 재현율 | 14/21, 66.67% | 13/21, 61.90% |
| 4등급 재현율 | 0/2 | 0/2 |
| 5등급 재현율 | 4/4 | 4/4 |
| 실제 4·5를 1·2로 낮춘 건수 | 0/6 | 0/6 |

4등급 검증 2장은 두 모델 모두 하나는 3등급, 하나는 5등급으로 판독했다. 학습 4등급이 10장뿐이라는 데이터 한계와 등급 경계 구분 실패는 확인했지만 원인을 확정할 수는 없다. validation 결과를 반복 선택에 사용하므로 이 수치를 최종 성능으로 해석하지 않는다.

전체 등급 지표 개선과 검토 대상 재현율 저하가 함께 나타났으므로 **서비스는 초기 모델로 유지**하고 후보를 별도로 남겼다. 최종 선택·합격선 확정은 test 결과를 열기 전에 한다. 고정 test와 현장 일반화 성능은 미검증이다.

## 검증과 수정

- 후보 체크포인트를 `Predictor`로 다시 읽어 validation 58장 추론: 선택 epoch의 confusion matrix와 정확히 일치했다.
- 독립 검토에서 `--output`을 바꾸면 기존 서비스 가중치를 덮을 수 있다는 문제를 발견했다. 초기/서비스/기존 실험 출력 경로를 실행 전에 거부하고, 저장 시 배타적 파일 생성을 적용했다.
- 경로 보호 테스트 2개 통과. 첫 실행에서 macOS 임시경로 `/var`와 `/private/var` 별칭을 동일시하지 않은 테스트 assert가 실패하여 resolve 기준으로 수정 후 통과했다. 실제 모델 코드의 경로 보호는 resolve를 사용한다.
- 실행 중 학습 프로세스는 수정 전 코드를 이미 로드했지만 기본 별도 출력 경로로 성공했고 서비스 가중치가 보존된 것을 확인했다. 경로 보호 수정은 후속 실행을 위한 것이며 재학습은 수행하지 않았다.
- `/health`의 연결 모델 버전이 초기 모델로 유지됨을 확인했다.
- 두 후보 모두 성능 합격선 확정 및 최종 test 전이다. 부식 등급 성능 완료로 보고하지 않는다.
