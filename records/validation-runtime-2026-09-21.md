# 실제 브라우저·파일 저장 독립 검증

검증일: 2026-09-21. 기능 확인과 모델 성능, 자동 브라우저 검사와 실제 비개발자 사용자 검수를 구분한다. 아래에서 실제 확인한 범위만 완료로 표시하며 남은 항목을 전체 완료로 합산하지 않는다.

최신 사용자 합의로 대표 전체 흐름의 약 1분은 AI 판독 대기를 제외한 업무 조작 시간으로 확정됐다. 사람 사용성 검수는 선택 피드백이며 기능 완료의 필수 관문이 아니다. 아래 과거 시점의 사람 검수 대기·시간 해석 보류 기록은 당시 상태로 보존하며 이 최신 합의를 우선한다. AI는 직접 화면·백엔드·파일 증거를 확인하고 발견한 결함의 개선을 계속한다.

## 환경과 입력

- 로컬 Next/React 운영 화면과 MySQL/MinIO API. 시작 기준 커밋 `46f8cd23b6032741d08310b0108b205df5ae9787` 위의 구현 담당 미커밋 운영 build를 검사했다. 최종 커밋 검증으로 표시하지 않는다.
- Playwright CLI 0.1.21의 실제 headed 브라우저로 조작·스크린샷 저장. 제품 코드는 수정하지 않았다.
- 공개 demo 5개를 각각 두 번 사용한 검증용 업로드 10개, 총 13,139,268bytes. 같은 사진의 반복 업로드이며 학습 데이터 10개 추가나 test 10장 평가가 아니다. 제품 담당의 별도 demo 10개와 합쳐 전체 화면 20개로 보일 수 있다.
- 검증 전용 포인트·계획·수정 사유에 `validation` 표시. 실제 현장 검사·보수 판단으로 사용하지 않는다.

## 실제 확인한 내용

| 항목 | 실행 증거와 결과 |
|---|---|
| 웹 화면 | 초기 404 이후 대시보드 정상 렌더링. API 미연결 상태와 사진 미선택 안내를 별도로 캡처했다. |
| 실제 업로드 | 브라우저 파일 선택으로 공개 demo 10개 업로드, 설비·랙·포인트 연결 후 백엔드 목록에 10개 조회. |
| 파일 실체 | 10개 모두 원본/메타데이터/백엔드 재조회 bytes/MinIO 직접 조회 bytes의 SHA-256 일치. 백엔드 10개 HTTP200. |
| 이미지 렌더링 | 목록의 이미지 10개 모두 실제 크기가 있는 로딩 완료 이미지로 표시. 목록·원본 상세 스크린샷을 직접 열어 확인했다. |
| 실제 AI 연결 | 업로드 10개 모두 판독 완료 및 모델/전처리 버전 확인. 파일명 정답을 반환한 결과로 간주하지 않고 실제 모델 담당의 평가와 분리한다. |
| 사람 수정·후속 표시 | 원래 AI4와 사람 수정3을 별도 유지. 재촬영과 사유·라벨링 후보 지정 후 브라우저 새로고침, 후보 목록에서 재조회. 수정자·사유·시각·전후값 보존 확인. |
| 계획·이력 | 계획 생성→캘린더→검사 완료→계획 이력 조회 실행. 이전에 지적한 계획 이력 조회 누락 수정 확인. |
| 계획에서 TA 연결 | 캘린더 이벤트에서 같은 포인트 상세로 이동, 보수 검토·관리 대상·TA 포함 저장 후 재조회. |
| 재시작 보존 | 제품 담당의 API/MySQL/MinIO 재시작 후 17:10 KST 독립 조회. 같은 포인트 1개·사진 10개·계획 1개의 전 필드와 각 전체 이력 모두 재시작 전과 일치. 기존 사진 UUID로 백엔드와 MinIO를 다시 읽어 10개 모두 원본 SHA-256 일치. |
| 보수 상태 흐름 | 재시작 후 같은 포인트의 검토→진행→완료를 UI에서 사유와 함께 각각 저장하고 한국어 전후 상태 이력을 재조회·캡처했다. 실제 보수 완료가 아닌 기능검사임을 사유에 표시했다. |
| 지정 해제·재연결 | 사진의 위치를 미확인으로 변경, 사람 수정 등급을 AI 그대로 사용으로 전환, 재촬영·후보 해제 후 목록과 API 확인. 같은 포인트와 후속 표시를 다시 지정해 변경 이력 보존 확인. |
| 당시 demo 전용 모드 선택 UI | 임의 파일 input 0개, 시연 사진 선택 버튼으로 승인 demo 5개만 선택되는 것을 직접 확인·캡처 후 취소. 이 단계에서 추가 업로드하지 않았다. 공개 모드 실제 추가 5장 업로드는 제품 담당의 별도 검사다. 이후 일반 업로드 승인에 따른 새 설정의 검증을 대신하지 않는다. |
| 관리·TA 해제/재지정 | 17:22 KST 기존 검증 포인트의 두 체크를 해제해 관리·TA 목록에서 사라짐을 확인·캡처. 포인트 관리에서 같은 포인트를 다시 열어 재지정한 후 목록 복귀 확인·캡처. |
| 외부 HTTPS 조회 | 검증 담당이 실제 외부 URL에서 root/health HTTP200과 모델·저장소 ready, demoMode:true를 확인. 기존 사진 UUID의 실제 다운로드 2,965,056bytes와 원본 SHA-256 일치 확인. |

배포 담당은 이후 실제 외부 HTTPS 브라우저에서 demo5 선택·업로드·AI 판독 및 새 썸네일 5개 렌더링, 정확한 Origin의 OPTIONS204를 별도로 확인했다고 보고했다. 이는 배포 담당의 실행이며 검증 담당의 독립 외부 쓰기 검사로 표시하지 않는다. 전체 저장 사진은 해당 추가 검사 후 30개이며 승인 demo5의 반복 업로드다.

민감정보 없는 공개 demo 화면을 실제 캡처했으며, 사진 UUID·objectKey·pointId와 4종 해시·이력 JSON·다운로드 파일을 로컬 증거로 보존했다. 상세 경로와 검증 데이터 식별자는 Git 제외 `validation-browser-2026-09-21.local.md`에 있다. 로그 PASS만으로 판정하지 않았다.

## 시간 관찰의 한계

업로드 POST의 브라우저 resource timing은 1,394.4ms, 첫 사진 DB 생성부터 마지막 실제 추론 완료까지의 서버 이력 차이는 14.38초였다. 전자는 전체 사용자 입력·화면 반영 시간, 후자는 최종 UI 렌더 시간까지 포함한 측정이 아니다. 각 1회 관찰이며 계약의 사용자 시간 목표 전체를 합격으로 선언하지 않는다.

## 첫 기록 당시 남은 검증

이 절은 첫 기록 당시의 미완료 항목이다. 이후 해소 여부는 후속 절과 문서 마지막의 현재 남은 항목을 따른다.

- 사용자가 공개 가능한 사진의 일반 업로드를 추가 승인했다. 첫 커밋 당시에는 변경 검증 전이었고, 후속 새 설정 검증은 아래에 별도로 기록했다. 앞선 demo 전용 결과는 당시 설정의 이력이다.
- API·DB·MinIO 재시작 후 데이터·바이트 독립 비교와 기존 UUID의 화면 재조회 캡처를 마쳤다. 재시작 후 추가한 보수 진행·완료와 해제·재지정 이력은 브라우저/API 재조회로 확인했으며, 이 추가 상태에 대한 두 번째 서비스 재시작 검사는 하지 않았다.
- 잘못된 파일·판독 실패와 복구, 처리 중 동시 사용자 조작 및 시간 목표 전체는 이 담당의 추가 검증이 필요하다. 제품 담당의 별도 검사와 혼동하지 않는다.
- 허브 보고로 동료가 외부 사이트에서 실제 리뷰 중이다. 이 담당이 자동 조작한 증거와 구분하며, 비개발자 팀원의 전체 시나리오 수행 및 막힘 수정 후 재확인 완료는 아직 확인하지 못했다.
- 모델 화면에 최종 test 60%로 사용자 목표 70% 미달이 표시된다. 실제 추론 기능이 동작해도 모델 성능 완료가 아니다. 평가 재실행이나 최종 test 기반 튜닝은 하지 않았다.

## 첫 인계 당시 시간·성능 판정

다음 표는 후속 일반 업로드·시간·격리 오류 검사 전의 상태다. 이후 증거는 아래 후속 절에 기록한다.

| 기준 | 이 담당의 확인 결과 |
|---|---|
| 검사 계획 30초 | 계획 기능은 확인. 실제 사용자 입력을 포함한 정확한 전체시간 미계측. |
| 10장 업로드 30초 | 실제 업로드와 파일 실체는 확인. POST 1.3944초만 계측하여 전체시간 합격은 미검증. |
| 10장 추론 3분·비동기 사용성 | 실제 10개 판독과 서버이력 14.38초 확인. UI 반영 완료까지의 정확한 시간·추론 중 동시 업무는 미검증. |
| 대시보드 수초·능동조작 1분 | 기능 화면 확인. 합의된 측정 구간의 정확한 계측과 사용자 해석 확정은 미완료. |
| 모델 정확도 | 모델 담당 최종 기록은 train 230/231=99.57%, test 6/10=60%. train 기준 충족, test 70% 기준 미달. 이 담당은 test를 재실행하지 않음. |
| 실제 사용자 검수 | 허브 보고상 동료의 외부 사이트 리뷰 진행 중. 전체 시나리오 및 수정 후 재검수 완료는 미확인. 자동 브라우저 조작이 이를 대체하지 않음. |

모델 담당의 최종 평가 문서와 로컬 가중치 SHA-256 `21245b2f2d8b478b7b2762c51e7882692babd062eb1de8ec305cb090a7906753`이 일치함을 독립 확인했다. 제품의 사진별 응답 모델·전처리 버전도 최종 평가 문서와 일치했다. 서비스가 해당 경로를 로드했다는 실행 증거는 모델 담당 기록에서 확인했으며, 실행 프로세스 메모리의 가중치를 별도 추출·검증한 것은 아니다.

## 후속: 공개 가능한 사진 일반 업로드

2026-09-21 17:31~17:35 KST, build `WENVNeaK_111_0UhGIV48`의 실제 외부 HTTPS 화면을 독립 검증했다. health에서 `demoMode:false`, `publicUploadsAllowed:true`, 모델·저장소 ready를 확인했다.

- 일반 파일 선택과 별도 시연 사진 버튼, 링크를 가진 사람이 조회할 수 있다는 공개 범위 안내를 확인했다.
- 실제 파일 선택기로 이미 공개 승인된 demo01 복제본 한 개를 선택하고 별도 검증 포인트에 저장했다. 원본 자료와 고정 test를 사용하지 않았고, 학습 데이터 추가가 아니다.
- 새 사진 1개와 연결 포인트가 저장됐고 실제 AI 판독이 완료됐다. 원래 AI4, 사람 수정 없음, 최종 모델·전처리 버전 일치를 API와 화면에서 확인했다.
- 2,965,056bytes의 원본·DB SHA-256·백엔드 실제 바이트·MinIO 실제 바이트·외부 HTTPS 다운로드 파일의 SHA-256이 모두 일치했다.
- 선택 화면과 실제 사진·AI·포인트·변경 이력 상세 화면을 캡처하고 이미지를 직접 열어 정상 렌더링을 확인했다. 선택·업로드·AI 완료의 전체 사용자 경과시간 합격은 선언하지 않는다.

새 사진 식별자와 저장 경로·각 해시·원본 응답은 로컬 증거에 보존한다. 이 검증은 앞선 demo 전용 설정에서 일반 업로드 설정으로 바뀐 부분의 확인이며 기존 전체 시나리오 검증을 반복한 결과가 아니다.

### 후속 관찰: 시연 사진 불러오기 지연

같은 외부 브라우저에서 17:36경 시연 사진 불러오기를 클릭한 뒤 17:38:37까지 선택 완료가 되지 않았다. 요청 목록에는 demo 5개 모두 HTTP200이 표시됐으나 응답 본문 수신 완료는 확인하지 못했다. 실제 업로드 시작 전인데 화면은 `업로드 0% / 저장 중`이며 취소·파일 선택 버튼도 비활성화된 상태였다. 화면을 직접 캡처·확인하고 제품 담당에게 전달했다. 해당 시도에서 저장 POST를 실행하지 않았고 중복 방지 UI 검증 통과로 기록하지 않는다.

제품 담당은 별도 API 반복·동시 요청에서 같은 포인트의 기존 시연 사진 ID 재사용과 사진·이력·MinIO 수 불변을 확인했다고 보고했다. 이 결과와 이 담당의 외부 브라우저 지연 관찰은 구분한다. 제품 담당이 로딩 상태·시간 제한을 수정 중이며, 수정 후 화면 재확인이 남아 있다.

### 시연 지연 수정 후 재확인

build `Vr-tqGxrMmRf4NDZKNHDq`에서 17:46 KST 실제 외부 UI를 다시 확인했다. 시연 사진 선택은 다운로드 대기 없이 완료됐고, `시연 사진 열기`는 포인트 ID만 담은 JSON 요청을 보냈다. HTTP200과 `existing` 5개를 반환한 뒤 기존 상세 화면이 표시되기까지 2.753초였다. 반환한 ID 5개가 모두 실행 전 목록에 있었고 전후 모든 필드가 같았다. 다른 사용자가 별도 포인트에 사진을 추가한 상황이므로 전역 사진 수 불변으로 표현하지 않는다. 앞선 지연 현상은 이 새 방식에서 재현되지 않았다.

## 후속 시간 계측과 10장 실제 판독

같은 build의 외부 HTTPS에서 승인 demo5를 두 번씩 사용한 10개, 총 13,139,268bytes를 일반 업로드했다. 별도 검증 포인트에 연결했고 학습 사진을 추가한 것이 아니다.

| 구간 | 실제 관찰과 판정 |
|---|---|
| 파일 선택을 포함한 전체 업로드 조작 | CLI가 파일 선택기에서 제어를 반환해 연속 계측 결과를 확보하지 못했다. 전체 사용자 시간 합격은 미검증이다. |
| 업로드 제출 후 저장 | 제품 UI가 `10장 새로 저장 · 29.1초`를 표시했다. 이 값은 제품 내부 계측이며 목록 반영까지의 독립 연속 계측으로 대체하지 않는다. P02의 정확한 종료 구간까지 합격은 미확인이다. |
| 서버 생성→10개 판독 완료 | 17:48:47.205~17:49:01.962 KST, 서버 기록 14.757초. 모두 실제 모델 `done`이고 모델·전처리 버전이 일치했다. |
| 10개 결과 UI | 17:50:43.195 KST에 표의 해당 10행 모두 실제 등급을 표시함을 확인했다. 첫 DB 생성 후 115.990초 이내의 확인 상한이며, 최초 화면 반영 순간의 정확한 지연은 아니다. 접수 완료 후 3분 안에 10개 결과가 보인 증거는 확보했다. |
| 비동기 화면 이동 | 저장 후 검사 계획 화면으로 이동했으며 업로드 완료 알림과 계획 화면을 같이 확인했다. 이동 시점과 추론 상태를 하나의 시계로 계측한 값은 확보하지 못해 처리 중 동시 업무 성능 전체 합격은 보류한다. |
| 계획 30초 | 계획 버튼 열기→제목·날짜·포인트·메모 자동 입력→저장→목록 반영 0.928초. 자동 조작 1회 기준 충족이며 실제 동료의 입력 속도를 증명하지 않는다. |
| 대시보드 수초 | 기존 브라우저에서 새로고침 시작→DOM 0.985초→수치 1.478초→모든 main 이미지 로드 1.483초. 1회 관찰이며 미합의된 수치 합격선을 새로 만들지 않는다. |

10개 등급 행·실제 화면 스크린샷, 입력 목록·서버 응답·구간별 결과를 로컬 증거로 남겼다. 기존 원본 이미지를 쓰던 버전의 불완전한 대시보드 측정 시도는 대기 timeout으로 중단됐으며 새 버전의 성공값과 혼합하지 않는다. 실제 비개발자 전체 시나리오와 능동 조작 1분 확인은 여전히 별도다.

## 격리 환경 오류 검사

공유 사이트를 중단하지 않고 제품 담당이 준비한 별도 Compose의 DB·MinIO·API·웹 및 판독 실패 프록시에서 검사했다. 이 환경의 결과를 공유 사이트에서 직접 실패를 일으킨 결과로 표시하지 않는다.

- 잘못된 PNG 내용의 파일을 실제 일반 업로드 입력에 지정했을 때 HTTP422와 `열 수 있는 JPG 또는 PNG를 다시 선택하세요` 안내를 확인했다. 전후 사진 5개와 모든 필드가 그대로여서 실패가 저장 성공으로 표시되지 않았다.
- 회사 원본과 무관한 64×64 합성 PNG를 사용했다. 승인 demo 해시 밖의 유효 이미지가 일반 업로드 HTTP201로 저장되어 demo 허용목록 제한 해제의 실행 증거를 확보했다. 분류 성능·학습 표본 증거가 아니다.
- 의도적인 모델 실패 뒤 같은 사진은 `판독 실패`, `원래 AI 등급: 결과 없음`, 연결 확인·재시도 안내와 `판독 다시 요청` 버튼을 표시했다. 실패 이력과 실제 사진 화면을 캡처·직접 확인했다.
- 모델 연결 복구 후 실제 재시도 버튼을 눌러 같은 사진 ID가 `done`으로 바뀌고 최종 모델·전처리 버전의 결과를 받음을 확인했다. 업로드→시작→실패→재요청→시작→완료의 6개 이력도 보존됐다. 복구 화면과 실제 다운로드 해시를 확인했다. 합성 이미지의 출력 등급은 연결 기능 확인에만 쓰며 등급 정답·성능으로 평가하지 않는다.
- 제품 담당이 격리 4개 컨테이너를 중단한 뒤 최신 API·웹 이미지로 재기동했다. 17:59 KST 독립 비교에서 사진 6개의 전체 필드, 합성 사진의 실패·복구 이력 6개, 같은 UUID의 실제 원본 다운로드 바이트가 재시작 전과 모두 같았다. 이전 공유 환경 재시작 확인과 별개로 새 일반 업로드 설정의 보존을 확인했다.
- 격리 API만 중단한 뒤 계획 저장을 눌러 실제 HTTP500과 한국어 서버 오류·저장 목록에서 반영 여부 확인 안내를 확인했다. 추가 진단용 응답 본문 추출은 브라우저 프로토콜 오류로 실패했으나 POST500 요청 기록과 실제 오류 화면은 확보했다. 본문 내용·형식까지 독립 확인했다고 주장하지 않는다.
- 이어 API를 복구하고 격리 웹만 중단한 상태에서 열린 계획 폼의 저장을 눌렀다. `서버에 연결할 수 없습니다`와 저장 목록 확인 후 재시도하라는 한국어 안내가 표시됐다. 이 경우도 화면을 직접 캡처·확인했으며 공유 서비스는 중단하지 않았다.

## 최종 P02·P03 계측 1회

2026-09-21 18:06:25.692 KST, 공유 build `vmIQA4WMXbo_KKkoz5t4G`를 고정하고 담당의 다른 업로드·추론 부하 없이 공개 demo 복제 10개를 한 번 더 검사했다. 다른 실제 접속자는 통제하지 않은 운영 중 관찰이다. 입력은 같은 승인 demo5의 반복 10개·13,139,268bytes이며 기존 검증 포인트에 연결했다.

파일·포인트 선택을 마친 뒤 실제 버튼 클릭 이벤트를 시작점으로 기록했다. 브라우저 DOM 변경 관찰로 업로드 모달이 닫히고 해당 이름 10개가 표에 나타나는 시각, 동일 10행 모두 등급을 표시하는 시각을 같은 브라우저 시계에 남겼다. POST resource timing도 따로 기록해 앞선 도구 실행 반환 중단과 구분했다.

| 구간 | 실측 |
|---|---:|
| P02: 업로드 클릭→저장된 10개 목록 표시 | 13.0127초 |
| 그 안의 POST 요청 | 12.3843초 |
| 업로드 클릭→10개 모두 등급 표시 | 30.1645초 |
| P03: POST 응답 완료→10개 모두 등급 표시 | 17.4515초 |
| 저장 목록 표시→10개 모두 등급 표시 | 17.1518초 |

처음 저장 목록이 표시될 때 10개는 모두 판독 대기였고 이후 모두 등급을 표시했다. 백엔드에서 해당 이름의 새 ID 10개가 전부 실제 모델 `done`임을 확인하고 화면을 캡처했다. **이 1회 관찰은 P02 30초 및 P03 3분 기준을 충족한다.** 파일 선택 전후 전체 조작 시간과 실제 사람의 입력 속도를 증명하는 결과는 아니다. 같은 관측으로 일반적인 성능 보장이나 모든 네트워크에서의 합격을 주장하지 않는다.

격리 오류 검사 이후 API·웹을 복구했고, 실패 안내 검사에 사용한 계획은 목록이 빈 상태로 실제 저장되지 않았음을 재조회했다.

## 현재 남은 항목

- 대표 전체 업무 흐름의 AI 대기 제외 약 1분 계측은 아래 후속 검사로 확인한다. 실제 비개발자 사용성 검수는 선택 피드백으로 남기며 기능 완료를 막지 않는다. 자동 입력 시간을 사람 실측으로 표현하지 않는다.
- 동시 업무는 아래 P05 후속 검사에서 실제 판독 대기 중 사진 검색·포인트 목록 조회, 실제 계산 중 계획 생성·저장·캘린더 표시까지 확인했다. 이 표본으로 모든 업무 경로의 성능을 보장하지 않는다.
- 모델 최종 test 6/10=60%는 사용자 기준 70% 미달이다. 실제 제품 기능과 별도로 남는 성능 실패이며 이 검증에서 모델을 재튜닝하거나 test를 재실행하지 않았다.
- 개념 3D 맵의 새 위치 매핑 검증은 허브·제품 담당의 별도 증거다. 이 담당의 사진·포인트 연결 확인을 맵 전체 요구 합격으로 확대하지 않는다.

## P05 후속: 실제 판독 대기 중 조회

2026-09-21 18:17~18:19 KST, 제품 담당과 순서를 조율해 격리 Compose 5개 서비스에서 기존 승인 demo01 한 장을 실제 UI로 재시도했다. API는 컨테이너 모델에 연결됐고 최종 동결 모델·전처리를 사용했다. 공유 사이트에 새 사진을 올리지 않았으며, 원본·test나 인위적 지연을 사용하지 않았다.

추론 작업이 없을 때 동일한 계획 조회→사진 검색→포인트 목록 조회를 실행했다. 다음 실제 재시도에서 API의 대상 사진 상태를 각 조작 전후 읽고, 조작 시작·종료를 같은 Mac 시계로 기록했다.

| 동작 | 추론 없음 | 재시도 후 | 겹침 증거 |
|---|---:|---:|---|
| 계획 조회 | 42ms | 139ms | 시작 직전 error, 완료 후 pending이므로 전체 구간이 대기와 겹쳤다고 주장하지 않음 |
| 사진 목록·이름 검색 | 83ms | 57ms | 18:19:04.985~18:19:05.042, 조작 전후 모두 같은 사진 pending |
| 포인트 목록 조회 | 67ms | 47ms | 18:19:05.058~18:19:05.105, 조작 전후 모두 같은 사진 pending |

포인트 화면을 실제 캡처한 뒤 18:19:05.235에도 같은 사진이 pending이었다. 서버 이력은 이후 18:19:05.388 processing→18:19:06.408 done을 기록했다. 후속 API와 상세 화면에서 동일 사진 ID·원본 해시·최종 모델 버전·실제 AI 결과를 확인하고 직접 스크린샷을 검토했다. 자동 대기 코드의 마지막 조회가 아직 pending이었던 값은 완료 증거로 쓰지 않았고, 후속 실제 재조회와 이력으로 완료를 확인했다.

**이번 1회에서 비동기 대기 중 사진 검색·포인트 조회의 응답 유지와 이후 동일 사진 판독 완료를 확인했다.** 해당 조작은 조회뿐이므로 중복 저장을 유발하지 않았으며 화면이 멈추는 현상은 관찰하지 않았다. 밀리초 차이를 성능 개선으로 해석하거나 모든 깜박임·모든 업무 경로를 검증했다고 주장하지 않는다. 동시 계획 생성, 실제 비개발자 전체 사용성·1분 기준 및 모델 성능 미달은 별도 항목이다.

### P05 보완: 실제 계산 중 계획 작성·저장

18:25 KST 제품 담당의 격리 5서비스 재시작 검사가 끝난 뒤 같은 환경을 단독 사용했다. 추론이 없는 상태에서 계획 버튼 열기→제목·날짜·포인트·메모 입력→저장→모달 닫힘·캘린더 제목 표시를 측정하고, 실제 판독 중 같은 흐름을 한 번 실행했다. 두 계획 모두 검증용 이름·메모를 썼다.

| 구간 | 무추론 기준 | 실제 계산 중 |
|---|---:|---:|
| 계획 작성 시작→저장·캘린더 표시 | 194ms | 203ms |
| 시작~종료 KST | 18:26:13.745~13.939 | 18:27:23.075~23.278 |
| 실제 저장 | HTTP201, 같은 제목 계획 ID 1개 | HTTP201, 같은 제목 계획 ID 1개 |

대상 demo04의 서버 이력은 18:27:23.041 processing→23.547 done이다. 계획 조작의 전체 구간이 이 실제 계산 구간 안에 포함됐고, 조작 직전·입력 후·캘린더 반영 후·스크린샷 후 23.363에도 동일 사진의 processing을 실제 API로 확인했다. 함께 대기한 demo05도 24.607에 정상 완료됐다. 두 결과는 동일한 동결 모델·전처리 버전이었다. 계획 UUID를 저장 응답과 재조회로 대조해 중복 생성이 없음을 확인했고, 저장 알림과 캘린더의 두 검증 계획을 실제 화면에서 확인했다.

이 검사에서 새 사진을 업로드하거나 모델에 인위적 지연을 넣지 않았다. 기존 실패 demo 두 장을 재시도해 짧은 실제 대기열을 사용했다. 앞선 준비 시도는 다른 기존 demo 두 장을 재시도한 뒤 도구 환경의 `setTimeout` 미지원 오류로 계획 조작 시작 전에 중단됐다. 지원되는 대기 API로 고친 뒤 남은 실패 사진 두 장으로 측정한 결과가 위 표다. 준비 중단을 숨기거나 그 시도를 성공 표본으로 포함하지 않으며, 총 네 장의 기존 demo를 재시도했지만 새로운 사진·학습 표본은 추가하지 않았다.

앞선 조회 검사와 합쳐 **이번 표본에서 P05의 계획 생성·목록/필터·포인트 조회가 비동기 판독을 기다리는 동안 계속 가능함을 확인했다.** 화면 멈춤이나 중복 계획 생성은 관찰하지 않았다. 자동 입력 1회 측정이므로 실제 사람의 전체 1분 검수나 통계적 성능 보장으로 해석하지 않는다. 모델 최종 test 60%의 기준 미달도 그대로 남는다.

## 알림·취소 계획 표시 수정 검증

2026-09-21 19:23~19:25 KST, 격리 API에 연결한 후보 웹 build `IhRdrULeZ8bhtVX0y6hS0`에서 두 표시 문제를 좁게 재검사했다. 실제 브라우저 조작과 DOM의 역할·색·아이콘을 확인하고 스크린샷 33~37을 직접 검토했다.

| 경로 | 실제 관찰 | 판정 |
|---|---|---|
| 계획 등록 성공 | HTTP201 뒤 `검사 계획을 저장했습니다.`, `role=status`, 녹색 `rgb(35, 76, 53)`과 체크 아이콘 | PASS |
| 포인트 없는 계획의 캘린더 안내 | 해당 계획 클릭 뒤 제목·메모, `role=status`, 청회색 `rgb(52, 72, 84)`과 정보 아이콘 | PASS |
| 사진 이력 조회 오류 | 기존 demo 사진 이력 GET 한 건에 브라우저에서만 HTTP500을 주입했다. 재열기 안내, `role=alert`, 적색 `rgb(132, 47, 47)`과 경고 아이콘이 표시됐고 주입 route는 finally에서 해제했다. | PASS |
| 취소 계획 목록·이력 | 기존 검증용 계획 한 개를 API에서 planned→cancelled로 한 번 변경했다. 목록은 회색 `취소` 배지이며 같은 ID의 이력은 `검사 예정 → 취소`, 수정자·시각·사유를 표시하고 기존 등록 이력을 보존했다. | PASS |

성공·안내 검사에 포인트 없는 검증용 계획 한 개를 새로 등록했다. 취소에는 기존 P05 기준 계획을 사용했고 다른 계획은 변경하지 않았다. 취소 PATCH 뒤 자동화가 캘린더와 목록의 같은 제목 두 곳을 찾아 중단된 시도는 보존했다. 후속 조회에서 목록 행으로 선택 범위를 좁혀 확인했으며 PATCH를 반복하지 않았다.

공유 서비스 중단·공유 데이터 변경·사진 업로드·모델 재실행은 없었다. 이번 오류는 브라우저에서 주입한 응답의 표시 검증이며 실제 공유 서버 장애 검사로 표현하지 않는다. 기존 업로드·재시작·P02·P03·P05 검사를 반복하지 않았다.

후보 검증 PASS를 제품 담당과 허브에 전달했다. 제품 담당은 같은 화면 소스 해시로 만든 공유용 build `EdmEp7APLdOriQwJm5AiZ`로 웹을 교체했다고 보고했으며, 후보에서 확인한 결과와 공유 배포 후 확인은 구분한다. 실제 비개발자 전체 흐름·능동 조작 1분 확인은 남아 있고 최종 test 60%의 목표 70% 미달도 그대로다.

## P06: 확정된 대표 전체 업무 흐름 계측

최신 사용자 합의는 전체 과정 약 1분에서 **AI 판독 대기만 분리**하는 것이다. 실제 사람 검수가 당장 없으면 AI가 화면·백엔드 증거로 검증과 개선을 계속하며, 사람 피드백은 선택 사항이다. 기존 P01 계획 작성 30초, P02 10장 업로드 30초, P03 10장 추론 180초, P05 동시 업무의 번호와 기존 증거는 유지하고 대표 전체 흐름을 P06으로 추가했다.

2026-09-21 20:41 KST, 격리 웹 build `IhRdrULeZ8bhtVX0y6hS0`에서 실제 headed Chrome 153과 Playwright CLI로 한 개의 연속 조작을 계측했다. 제품 담당은 화면 소스가 커밋 `67ea22`와 해시 일치하고 API 소스가 기존 검증 이미지와 차이 없음을 확인해 인계했다. 실행 구성은 Mac 웹 한 개와 격리 Docker API·MySQL·MinIO·모델 네 개이며 공유 서비스와 분리돼 있다. 기존 서비스 기동·브라우저 연결 뒤 시작했고, 앞선 중단 시도의 실제 추론이 끝난 warm 상태였다.

입력은 승인 demo01 복제 한 개, JPEG 2,965,056bytes다. 입력 파일·문안을 미리 준비한 뒤 **업로드 창 열기부터 파일 입력 선택, 새 설비·랙·포인트 입력까지 타이머에 포함**했다. 실제 업로드와 목록 표시→같은 사진 AI 결과 확인→선택 등급 수정·재촬영 사유 저장→같은 포인트의 후속 계획 생성→계획 이력→사진 검색·상세·변경 이력 재조회 순서로 조작했다. 모든 선택 기능의 반복 토글이나 실제 현장 판단을 수행한 것은 아니다.

| 경계 | 동일 자동화 프로세스 시계의 실제 시각·시간 |
|---|---|
| 전체 시작→마지막 사진 이력 표시 | 20:41:05.087~20:41:07.911 KST, 2.824초 |
| 파일 선택·포인트 입력 완료 | 20:41:05.156 KST |
| 업로드 클릭→HTTP201 | 20:41:05.157~20:41:05.984 KST, 0.827초 |
| 저장 목록 표시 | 20:41:06.015 KST, 대상 사진은 판독 대기 |
| 다른 조작 없이 AI 결과 표시만 기다림 | 20:41:06.016~20:41:06.807 KST, 0.791초 |
| AI 대기를 차감한 업무 조작 | 2.824−0.791 = **2.033초** |
| 그 안의 계획 작성→저장·목록 표시 | 20:41:07.205~20:41:07.324 KST, 0.119초 |

AI 처리와 다른 조작의 겹침은 이 시도에서 없었다. AI-only 구간은 순수 모델 연산 시간 대신 결과가 UI에 나타날 때까지 아무 조작 없이 기다린 시간이며 UI 갱신 지연도 포함한다. 업로드, 일반 저장·조회와 계측 중 두 번의 스크린샷 저장은 업무 시간에 포함했다. 백엔드 증거 수집·파일 해시 대조·최종 스크린샷 직접 검토는 타이머 종료 뒤 수행했다. **이 자동 조작 1회는 약 1분 목표를 충족하지만, 사람의 입력·인지·등급 판단 시간이나 현장 사용성을 실측한 결과가 아니다.**

실제 API·화면 대조 결과는 다음과 같다.

- 새 사진 HTTP201→실제 동결 모델 `done`, AI 4등급과 사람 수정 3등급이 분리 보존됐다. 재촬영 지정과 사유도 같은 사진에 저장됐다. 등급 수정은 기능 검증용이며 정답 판정이 아니다.
- 같은 사진 ID의 이력 네 개가 등록→실제 추론 시작→완료→판단 수정으로 이어졌고, 수정자·시각·사유·전후 값이 화면과 일치했다. 후속 계획은 HTTP201, 사진과 같은 포인트 ID, 예정일·메모·등록 이력이 보존됐다.
- 승인 원본, DB SHA-256, 백엔드에서 같은 사진 ID로 내려받은 실제 파일의 해시가 모두 일치했다. 이번에는 MinIO 직접 다운로드 검사를 반복하지 않았다.
- 스크린샷 41~44를 직접 확인했다. 최초 상세 캡처 41은 원본 로드 중 일부만 보였으므로 완전한 원본 렌더링 증거로 쓰지 않는다. 마지막 상세 43에서 전체 원본을 확인했고, 후속 읽기에서 이미지 `complete=true`, 실제 크기 5568×4872를 확인했다. 42의 계획 이력과 44의 전체 사진 이력도 직접 대조했다.

실패·중단 시도를 숨기지 않는다. 첫 준비 실행은 도구의 `URL` 전역 미지원으로 타이머·데이터 변경 전에 중단됐다. 다음 첫 계측에서는 업로드·실제 판독까지 성공했으나 등급 선택기의 접근성 이름에 도움말이 포함되어 자동화의 정확 일치 선택기가 30초 timeout으로 중단됐다. 이때 만든 사진·포인트는 보존했고 판단·계획 저장은 발생하지 않았다. 실제 화면의 이름으로 선택기를 고친 뒤 다른 검증용 이름으로 위 유효 표본 한 번을 계측했다. 중단 표본을 제품 결함이나 성공 시간으로 합산하지 않는다.

이번 작업 전체의 격리 데이터 변화는 승인 demo 복제 사진 두 개, 검증 포인트 두 개, 계획 한 개, 사진 판단 저장 한 번이다. 공유 사이트 쓰기·회사 원본·고정 test 사용·모델 변경은 없고 새 제품 결함은 발견하지 않았다. 제품 담당에게 검증 환경 사용 종료를 전달했다. 사람 검수는 선택 피드백으로 남기며 모델 최종 test 60%의 목표 70% 미달은 별도 성능 실패로 유지한다.

## 상세창 이력·저장 응답 경합 수정 전후 검증

P06 종료 후 허브·제품 담당이 배정한 두 결함을 별도로 확인했다. 20:46 KST 기존 후보 `IhRdrULeZ8bhtVX0y6hS0`에서 사진 A의 이력 응답을 브라우저에서 보류하고 창을 닫은 뒤 포인트 B를 열었다. B의 실제 이력을 확인한 다음 A 응답을 풀자 B 창의 이력이 A 사진 이력으로 바뀌었다. 이어 사진 A 저장 응답을 보류→닫기→사진 B에 미저장 사유 입력→A 응답 완료 순서에서 B 창이 닫혀 입력이 사라짐을 실제 화면으로 재현했다.

제품 담당이 이력을 해당 상세창에 결합하고 닫힌 창의 완료 동작을 제한하는 수정과 빌드를 준비했다. 허브의 독립 코드 검토 PASS 보고와 아래 실제 화면 검증은 별도 증거다. 20:52~20:53 KST 새 후보 build `T7stOJVGyEBFeKVD1VBhc`에서 다음을 확인했다.

| 경로 | 실제 화면 결과 |
|---|---|
| 늦은 사진 A 이력→열린 포인트 B | A 응답 해제 후 B의 이력 텍스트가 전후 동일했고 A 이력이 나타나지 않았다. 늦은 오류 알림도 없었다. |
| 닫힌 사진 A 저장→새 사진 B | A 응답 해제 후 B 창 한 개와 미저장 사유 입력값이 그대로 유지됐다. |
| 닫힌 포인트 A 저장→새 포인트 B | 같은 방식으로 B 창과 미저장 사유가 유지됐다. |
| 닫힌 계획 A 저장→새 계획 B | 같은 방식으로 새 계획 창과 미저장 제목이 유지됐다. |
| 현재 열린 사진의 이력 실패·복구 | 브라우저 HTTP500 응답에서 창은 유지되고 한국어 재열기 안내와 `role=alert`가 표시됐다. 주입 해제 후 다시 열어 해당 사진의 실제 이력을 조회했다. |
| 현재 열린 사진의 정상 저장 완료 | 브라우저 HTTP200 응답에서 성공 알림 `role=status`와 원래 창의 정상 닫힘을 확인했다. |

지연 응답 해제 후 각 새 창을 1초간 관찰하고 DOM·스크린샷을 대조했다. 수정 전 스크린샷 45~47과 수정 후 48·50~54를 직접 확인했다. 사진·포인트 PATCH와 계획 POST는 브라우저에서 응답을 만들어 실제 서버로 전달하지 않았고, 모든 가로채기는 finally에서 해제했다. 사진 10개·포인트 24개·계획 4개와 검사 대상 JSON의 전후 불변을 확인했다. 새 사진·업무 저장·모델 실행·공유 서비스 중단은 없었다. 따라서 이번 정상 저장 확인은 UI 완료 처리의 회귀 검사이며 실제 DB 저장을 새로 검증한 결과가 아니다.

**후보의 두 재현 결함과 공통 완료 처리·오류 안내 검사는 PASS**다. 결과와 원자료를 제품 담당·허브에 전달하고 격리 환경 단독 사용을 해제했다. 공유 배포·외부 접속 확인은 제품 담당의 후속 작업이며 이 후보 검증으로 대신하지 않는다. P06을 재측정하거나 모델 성능 결과를 변경하지 않았다.
