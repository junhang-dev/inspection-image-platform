# AI 작업 기준

먼저 `README.md`와 `docs/goal.md`를 읽고 현재 파일·Git 상태를 확인한다. 최신 사용자 합의를 우선하며, 참고 그림의 모든 구성 요소를 이번 구현 요구로 확대하지 않는다. 설명과 결과 보고는 한국어로 짧게 한다.

최신 최종 제품 확장은 `docs/product-v2.md`를 함께 따른다. 계획 우선 사진 흐름, 팀·랙·포인트 가상 지도, 워크리스트, 복구 가능한 사진 삭제와 업로드 처리 개선은 승인된 범위다. 기존 검증을 새 UI의 PASS로 대신하지 않는다.

## 진행과 협업

- 합의한 범위의 구현·검증은 진행한다. 작은 실행 단위부터 확인하고, 실행하지 않은 기능·검사·성능을 완료로 표시하지 않는다.
- 같은 파일의 편집 담당은 한 명, Git 작성도 동시에 한 명이다. 독립적인 작업만 분리하며 다른 작업의 수정·index를 대신 커밋하지 않는다.
- 제품 코드 통합은 Jun이 맡는다. 허브는 우선순위와 결과 회수를 맡고 구현은 담당 작업에서 진행한다. 작업별 브랜치를 사용하고 변경 경로를 골라 stage한다.
- 필요한 중요한 변경에는 독립 기술 검토를 활용한다. 사소한 수정마다 하위 에이전트 검토를 강제하지 않는다. 검토 의견은 사용자 승인이나 플랫폼 권한을 대체하지 않는다.
- 목표·등급 기준 변경, 추가 비용, 새로운 외부 전송 목적지, 중요한 원본 삭제, 계정·권한 확대는 사람에게 판단을 요청한다. 기존에 승인된 동일 목적·대상·범위를 반복해서 묻지 않는다.
- 승인 대기 중에도 독립적인 허용 작업은 진행한다. 거절된 행위를 다른 도구로 우회하지 않는다. 강제 push·이력 초기화·동료 변경 삭제를 자동 수행하지 않는다.

## 데이터와 AI

- 공개 자료는 승인된 `ref/` 그림 6개와 `image/demo/` 샘플 5개, 사용자가 이번 지도 구현에 제공한 캡처 기반 보강 자산이다. 지도는 AI 보강 참고 이미지이며 팀 구역·랙·좌표는 가상이라고 표시한다. `image/`의 원본 300장·부가 자료, 기존 train/validation/test 분리와 원본 이름·내용을 보존한다. demo 복제본을 새 학습 표본으로 중복 집계하지 않는다. 원본300의 공유 사이트 공개는 별도 명시 답변 전까지 금지하며 균등 배치 요청을 공개·재학습 승인으로 해석하지 않는다.
- 고정 test 10장을 샘플 선정·튜닝에 사용하지 않는다. 파일명에서 학습 라벨을 정리하는 것과 실제 모델 예측을 구분하고, 파일명의 정답을 예측값으로 반환하지 않는다.
- 원래 AI 결과와 사람 수정값을 구분한다. 모르는 위치는 미확인으로 남기고, 시각적 부식 등급을 설비 안전성·잔여 두께·보수 지시로 확대 해석하지 않는다.
- 인증 파일·과제와 무관한 회사 문서·개인 대화·전역 설정·로컬 `.codex/`·DB·모델 가중치·의존성을 공개하지 않는다. Git에서 제외된 파일도 내용과 목적지를 확인 없이 외부로 보내지 않는다.
- 지도 보강 PNG 2개는 Git 담당 작업의 직접 사용자 승인과 게시 검토를 거쳐 PR #5에 게시됐고 원격 커밋도 확인했다. 동일 지도 자산의 제품 연결은 진행한다. 앞서 거절된 허브의 작업 간 승인 전달을 우회 승인으로 사용하지 않으며, 실제 게시 사실과 구분한다. 원본 300장의 공유 사이트 공개는 지도 승인에 포함되지 않는다. 실제 팀 경계가 없어도 가상 구역임을 표시하고 구현·검증을 계속한다.

## 완료와 기록

- `docs/goal.md`의 완료 기준과 실제 결과를 비교한다. 기능 완료와 모델 성능 완료를 나누고, 실패·미검증·남은 사람 판단을 보고한다.
- 실제 행사 기록은 `records/`에 남긴다. 시작 시각·커밋·목표와 실제 프롬프트·검사·버전·한계를 기록하며, 사전 준비를 행사 실행 증거로 만들지 않는다.
- 전체 업무 조작 약 1분에서 AI 판독 대기는 제외한다. 실제 사람의 시간과 에이전트 브라우저 조작 시간을 구분한다.
- 사용자·동료 검수는 선택 피드백이다. 결과가 없다고 대기하거나 정체 처리하지 않고 기존 범위의 사용성·오류 복구·데이터 보존을 자율 개선한다. 근거 없는 기능 추가나 완료 검사 반복은 피하고, 실제 화면·백엔드 파일 증거로 개선을 확인한다. 사람 검수 결과를 대신 만들어 기록하지 않는다.
- 검증된 변경은 승인된 작업 브랜치에 게시하고 관제를 유지한다. 자동 pull·commit·push 전용 예약은 만들지 않는다. 사용자의 중단 지시 또는 별도 종료 합의를 따른다.
